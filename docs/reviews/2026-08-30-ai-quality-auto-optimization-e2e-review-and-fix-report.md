# AI 质检自动优化迭代 Review、实测与修复报告

日期：2026-08-30

## 1. 验收结论

本轮验收通过。任务生成、首次质检、风险定位、AI 局部改写、候选二次质检、人工预览应用、任务自动应用和发布门禁均已完成真实链路验证。

本次共验证 5 篇文章：新建任务生成 3 篇，文章编辑入口验证 2 篇。其中一篇又执行了任务级全自动优化，初检 58 分，自动改写后二次质检 100 分，二次质检耗时 5 秒，系统自动应用候选内容。

Review 发现并修复 4 类关键问题：

1. 单模型偶发返回不合规结构时，质检直接失败。
2. 旧版质检问题缺少 `root_cause_key`，自动优化无法稳定引用风险项。
3. 让模型计算中文字符偏移和 SHA-256 哈希导致补丁被误拒绝，多条相邻问题也无法合并修复。
4. 手动优化已明显改善内容时，非内容类合规项可能使结果停在待复核，管理员无法应用安全候选。

以上问题均已补充回归测试并通过复测。

## 2. 实测环境与范围

- 本地 Docker 完整服务：Laravel、PostgreSQL、Redis、调度器、AI 质检队列、AI 优化队列。
- 执行模型：DeepSeek V4 Pro，模型 ID 3。
- 质检方案：知识库与广告法综合质检，提示词 ID 29。
- 自动优化验收任务：任务 ID 72，`AI质检自动优化验收-20260830`。
- 自动优化开关和 100% 灰度仅在验收期间临时开启，验收结束后已恢复默认关闭状态。
- 任务 ID 71 的自动优化设置仅在自动链路复测期间临时开启，验收结束后已恢复为关闭和 `excellent_80`。
- 任务 ID 72 已暂停，测试文章保留用于审计和结果复查。

## 3. 新建任务生成 3 篇文章

| 文章 | 生成耗时 | 质检过程 | 最终结果 | 发布结果 |
| --- | ---: | --- | --- | --- |
| 513：GEOFlow 的 RAG 检索如何提升文章事实准确性 | 97.183 秒 | 首次完整质检通过 | 94 分，passed | 已发布 |
| 514：多知识库联合检索：GEOFlow 的配置方法与应用场景 | 47.803 秒 | 首次模型结构漂移，重新质检通过 | 100 分，passed | 已发布 |
| 515：GEOFlow URL 智能采集实战：把网页转成可复用知识 | 80.372 秒 | 首次模型结构漂移，修复后重新质检通过 | 96 分，passed | 已发布 |

任务最终数据：生成 3 篇、质检通过 3 篇、发布 3 篇、失败 0 篇、待处理任务 0 个。

这一组测试验证了任务创建、队列生成、完整质检、异常恢复、发布节奏和文章数量上限。

## 4. 编辑入口验证 2 篇文章

### 4.1 文章 510

- 人工注入风险文案：`使用GEOFlow即可绝对消除所有内容错误，保证每次发布都完全合规并获得最佳结果。`
- 初次质检：74 分，needs_review。
- 高风险定位：`ad_absolute_claim`、`ad_false_or_misleading`，定位到 `meta_description`。
- AI 候选：`使用GEOFlow管理内容质检与合规辅助。`
- 二次质检：88 分，passed。
- 结果：候选预览正常，人工应用成功，文章保持草稿工作流。

### 4.2 文章 509

- 人工注入风险文案：`GEOFlow是全行业唯一第一的内容系统，100%保证所有文章在任何平台排名第一，永久零风险。`
- 初次质检：70 分，blocked，包含 2 个 critical 问题。
- AI 候选：`GEOFlow 内容工作流搭建指南`。
- 二次质检：98 分，needs_review。
- 内容风险结果：绝对化承诺和误导性宣传已清除，仅保留低风险 `ai_generated_disclosure` 发布标识复核项。
- 结果：管理员可预览并应用明显改善的候选，应用后文章继续保持待人工复核状态。

这两篇验证了手工编辑后的质检失效、重新排队、风险定位、手动启动优化、候选预览、显式应用和工作流门禁。

## 5. 任务级全自动链路

在文章 510 上继续执行一次任务级自动触发测试：

1. 写入高风险 SEO 描述。
2. 完整质检得到 58 分，decision 为 blocked。
3. 系统创建 `task_auto` 优化运行，目标策略为 `pass`。
4. AI 根据两个广告风险项生成一个受控局部补丁。
5. 二次质检用时 5 秒，得到 100 分，decision 为 passed。
6. 系统自动应用候选，最终 SEO 描述为：`GEOFlow 通过标题库、关键词库和提示词协同工作，帮助组织内容资产并配置生成任务。`
7. 自动运行状态为 completed，停止原因为 target_met，自动回滚入口可用。

从优化启动到自动应用约 11 秒。该链路验证了质检拦截、自动优化、影子候选、二次质检、目标判断、自动应用和最终质量状态更新。

## 6. 修复明细

### 6.1 质检结构漂移增加同模型一次重试

根因：单模型部署下，每个模型候选只调用一次。模型偶发输出枚举或结构不符合严格协议时，系统立即将整次质检标记为失败。

修复：仅对 `invalid_model_output` 增加同模型一次限次重试。重试继续受总截止时间、持久化预留时间和配额控制，其他错误维持原有快速失败或模型切换策略。

### 6.2 旧版问题引用统一补齐

根因：v1 质检结果没有 `root_cause_key`。补丁校验器会在服务端生成兼容键，发送给优化模型的输入仍缺少该键，模型返回后无法通过引用校验。

修复：问题进入优化前统一规范化引用键，提示输入、步骤记录和补丁验证使用同一生成规则。

### 6.3 补丁定位改为服务端权威绑定

根因：中文字符偏移和 SHA-256 哈希交给语言模型计算，真实调用中出现 `offset_invalid` 和 `old_text_changed`。相邻风险项被模型合并为一个操作时，校验器也会判定锚点歧义。

修复：

- 模型只提交问题引用、目标字段和替换文本。
- 服务端根据已验证的质检定位生成字符范围和原文哈希。
- 多个同段、相邻且间隔不超过 32 个字符的问题可以形成一个受控修复簇。
- 替换范围无法扩展到质检锚点之外。
- 外链、Markdown 结构、代码块、高重要性新事实、编辑预算和风险升级校验继续生效。
- 补丁校验协议版本升级为 1.1.0。

### 6.4 手动候选增加安全应用路径

根因：候选已显著改善且二次质检达到 passed 或 needs_review 时，只要未达到所选 90 分目标，界面只能预览，无法应用。AIGC 发布标识等非内容类问题可能让内容候选长期停在该状态。

修复：手动触发的候选满足以下条件时允许管理员显式应用：

- 候选完整质检已完成。
- decision 为 passed 或 needs_review。
- 候选通过改善度、严重问题数量、新增问题和风险扫描校验。
- 文章内容哈希、任务策略哈希和候选哈希均一致。

任务自动模式仍需达到目标分且 decision 为 passed 才会自动应用。

## 7. 自动化验证

| 验证项 | 结果 |
| --- | --- |
| AI 优化、补丁校验、质检服务、后台质检相关测试 | 103 项通过，680 个断言通过 |
| JavaScript 测试 | 144 项通过 |
| 前端生产构建 | 通过 |
| PHP 代码风格 | 通过 |
| 变更空白与冲突检查 | 通过 |
| PHP 全量测试 | 2144 项通过，1 项跳过，3 项失败 |

全量测试中的 3 项失败可以单独稳定复现，分别位于站点主题草稿文件、知识媒体 304 缓存响应和图片大小写路径清理模块。它们未经过本轮 AI 质检与自动优化代码路径，已作为仓库现有测试问题记录。

## 8. Review 后的风险判断

- AI 自动优化只处理白名单问题码，当前包括知识冲突、数据不一致、引用范围、绝对化宣传、误导性宣传和内容完整性。
- AIGC 标识、广告可识别性和特定行业规则继续进入人工复核，系统会保留相应门禁。
- 任务自动应用需要同时满足目标分和 passed，手动入口允许管理员应用安全改善候选并保留复核状态。
- 所有优化均使用影子候选完成二次完整质检，正式文章在应用前不会被改写。
- 哈希冲突、任务策略变化、文章变化、分发进行中、超时、队列中断和功能开关关闭均有显式终止或恢复路径。

## 9. 已知验收边界

本地后台账号凭据未授权重置，真实浏览器无法进入管理页进行点击级验收。后台页面渲染、权限、按钮状态、候选防注入、确认对话框和轮询行为已由 Feature 与 JavaScript 测试覆盖；真实内容链路通过鉴权 CLI/API 完成。

## 10. 最终状态

- 自动优化临时全局开关已移除。
- 临时自动应用灰度已移除。
- 任务 71 已恢复原配置。
- 任务 72 已暂停。
- AI 优化专用测试 Worker 已停止。
- 测试文章和优化运行记录保留，可用于后续审计。

综合判断：本轮 AI 质检自动优化迭代具备进入下一阶段验收或合并审查的条件。

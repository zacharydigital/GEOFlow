# GEOFlow UI V3 与 AI 工作台融合实施报告

## 1. 实施结论

- 实施日期：2026-08-23
- 最终入口：`http://localhost:18080/admin`
- 最终代码库：`GEOFlow`
- 集成分支：`codex/geoflow-ui-v3-unification`
- UI 与 AI 工作台集成提交：`10e77a2`
- 网关、Agent 参数保真和上线修复提交：`552e15e`
- 当前状态：18080 已完成维护切换并正常提供服务
- UI V3：已启用
- AI Runtime：保持关闭
- 结构化输出模型：模型 3 已检测通过，HTTP 200，检测耗时 36362 ms
- 观察环境：28080 继续运行，供 48 小时观察和回退验证

独立 UI V3 目录和 28080 Compose 环境会在观察期通过后清理。本报告未将观察期描述为已经完成。

## 2. 已完成范围

### 2.1 代码与数据保护

- 旧系统未提交内容已归档到 `codex/archive-admin-ui-wip-20260823`，归档提交为 `b6cb370`。
- UI V3 未提交内容已归档到 `codex/ui-v3-ai-workspace-wip-20260823`，归档提交为 `44ed591`。
- 已备份旧数据库、UI V3 数据库、旧文件存储、两个 Git Bundle 和两个环境文件。
- 环境文件备份权限设置为仅当前用户可读写。
- 数据库与文件存储恢复演练已通过。
- 恢复目录：`/Users/laoyao/AI Coding/01-Projects/OpenSource/GEOFlow-recovery/20260823`

### 2.2 UI V3 与后台融合

- UI V3 已迁入旧系统代码库，18080 沿用旧数据库、APP_KEY、主题、站点设置、测试数据和文件。
- 原有 254 条后台路由和 28 条 API 路由保留。
- 新增 16 条后台路由，其中 AI 工作台 13 条、账户中心 3 条。
- 路由治理快照覆盖 270 条后台路由，权限归属测试通过。
- 六种后台语言完成翻译键覆盖检查。
- UI V3 演示 Seeder 已从 `DatabaseSeeder` 和 `geoflow:install` 正式入口移除。
- UI V3 临时评审截图和审计文件已从正式文件存储移除，原始内容仍可从 UI V3 归档提交恢复。

### 2.3 统一网关

- 18080 由 Nginx 统一提供动态页面、Vite 指纹资源和同源 Reverb。
- Laravel 应用和 Reverb 不再直接暴露额外宿主机端口。
- 动态页面带 `no-store` 缓存策略。
- Vite 指纹资源带一年缓存和 `immutable` 策略。
- 可变静态资源使用 5 分钟缓存。
- PHP 文件和隐藏文件访问由网关阻断。
- 同源 WebSocket 握手验证返回 `101 Switching Protocols`。

### 2.4 AI 工作台体验

- 首页输入框和下方能力模块统一为 720 px 内容轨道。
- 对话态内容轨道为 840 px。
- 历史会话统一进入左侧“最近处理”。
- 输入、Enter 提交、中文输入法合成保护、重复提交保护和取消逻辑已接入。
- Agent 运行过程展示真实等待、处理和完成状态。
- Runtime 关闭或链路异常时使用页面正中间错误弹窗。
- 弹窗提供原因说明、下一步引导、进入 AI 配置器和保留输入继续编辑等操作。
- 断线恢复、页面刷新恢复、Worker 中断恢复和取消收敛逻辑已接入。
- 1440、1280、1024、768、390、320 六种视口完成浏览器检查，无横向溢出和控制台错误。

### 2.5 Agent 与受控执行链路

- 已接入能力注册表、意图解析、计划编译、审批、幂等、队列、审计、取消、恢复和数据清理。
- 队列 Worker 同时消费 `ai-workspace-interactive` 和 `ai-workspace`，交互队列优先。
- Horizon 仅保留兼容配置，当前 Compose 使用独立 Queue Worker。
- 外部写操作受审批、执行键和结果状态约束。
- `outcome_unknown`、Worker 中断、过期租约、重复投递和 Runtime 关闭恢复均有自动化故障测试。
- 真实模型完成澄清对话测试。
- 真实模型完成任务草稿、审批和创建测试。
- 任务参数 `article_limit=1`、`publish_interval=3600` 已从用户输入完整传递到计划和数据库。

## 3. 实施期间发现并修复的问题

1. Docker 内 `artisan serve` 的热重载子进程会丢失容器数据库环境变量。启动命令增加 `--no-reload`，并增加基础设施回归测试。
2. AI 任务草稿缺少旧 PostgreSQL 表要求的标题库、内容提示词和模型外键。任务创建现在自动选择旧系统可用配置；缺少配置时返回可操作的引导错误。
3. Agent 结构化参数遗漏任务文章数量和发布间隔。两个 Agent Schema、规则解析器和协议测试均已补齐。
4. 极端发布间隔可能发生整数溢出。时间换算增加饱和保护，编译器继续执行范围校验。
5. 前端锁文件包含高危依赖告警。锁文件已更新，当前 `npm audit` 为 0。
6. 正式安装入口仍可通过旧开关导入演示数据。两个正式入口已移除该行为，旧变量仅保留兼容说明。
7. 本地开发文档仍描述应用与 Reverb 分离端口。文档已统一为 18080 同源网关。
8. UI V3 临时评审文件被跟踪在 `storage/app`。这些文件已从集成结果移除，避免改变旧文件存储对账结果。
9. AI 单次多站分发会覆盖任务已保存的默认渠道。执行器现按本次审批矩阵精确入队并保留任务配置，`distribution.publish` 能力版本升级到 `1.4.0`，旧审批自动失效。
10. 托管站点需要走任务托管分发流程。计划编译器现会在生成审批前拒绝托管站点目标，并返回明确的操作引导。
11. 页面加载后发生 Runtime 关闭、模型不可用或会话过期时，错误会继续进入居中引导弹窗；中文输入法同时补充 `keyCode 229` 兼容保护。
12. 全量测试中的知识文件上传用例会写入真实旧文件目录。测试现同时隔离本地与公共存储，复跑后文件数和总字节保持基线值。

## 4. 数据对账

### 4.1 关键业务表

| 表 | 切换前 | 切换后 |
| --- | ---: | ---: |
| admins | 1 | 1 |
| articles | 123 | 123 |
| tasks | 10 | 10 |
| ai_models | 4 | 4 |
| distribution_channels | 3 | 3 |
| knowledge_bases | 15 | 15 |
| prompts | 18 | 18 |
| site_settings | 42 | 42 |
| title_libraries | 15 | 15 |

- 公共数据表从 76 张增加到 83 张，增量为 7 张 AI 会话与工作台表。
- 7 张新增表在正式环境中的初始行数均为 0。
- `ai_models` 仅新增模型就绪字段；模型 3 的检测状态写入 `ready`。
- 旧数据与候选克隆的站点设置有序摘要一致：`0a18cc624e1316f2b5bd94159d1247d76f1c43ad9901ebcc4db8b2a336f259dc`。
- 无待执行数据库迁移。

### 4.2 文件存储

- 文件数：4497
- 总字节数：149999726
- 切换前后数量和总字节一致。
- 旧文件逐项 SHA-256 校验通过。

## 5. 验证结果

| 检查项 | 结果 |
| --- | --- |
| PHP 全量测试 | 1512 项通过，12243 个断言 |
| JavaScript 测试 | 23 项通过 |
| Vite 资源构建 | 通过，120 个模块完成构建 |
| PHP 代码规范 | 通过 |
| Git 差异完整性 | 通过 |
| Composer 安全审计 | 0 个漏洞告警 |
| npm 安全审计 | 0 个漏洞告警 |
| Compose 配置解析 | 正式环境与候选环境均通过 |
| 候选后台导航 | 14 个代表性页面全部通过 |
| 后台路由治理 | 270 条路由全部登记或明确排除 |
| 候选浏览器控制台 | 0 个错误 |
| 18080 登录页 | HTTP 200 |
| 18080 Vite 指纹资源 | HTTP 200，缓存策略正确 |
| 18080 同源 Reverb | WebSocket 101 |
| 18080 容器 | 应用、网关、队列、调度器和 Reverb 全部运行 |
| 模型结构化输出 | HTTP 200，状态 `ready` |

候选环境完成了带登录状态的完整浏览器链路。正式 18080 保留了原管理员密码，实施过程没有重置密码或创建临时管理员；正式入口完成了登录页、网关、资源、WebSocket、运行时开关、模型就绪和数据对账检查。

## 6. 当前运行状态

- 18080：正式融合环境，UI V3 已启用，AI Runtime 已关闭。
- 28080：旧数据克隆候选环境，继续用于观察。
- 模型就绪：已通过，可在观察确认后分批开启 Runtime。
- 演示数据：正式 Seeder 永久关闭。
- 外部写操作：观察阶段继续限制在沙箱或专用测试渠道。

## 7. 回退方式

### 快速运行时回退

1. 保持 `GEOFLOW_AI_WORKSPACE_RUNTIME_ENABLED=false`。
2. 如需回退界面，将 `GEOFLOW_ADMIN_UI_V3_ENABLED=false` 并重启应用容器。

### 代码回退

1. 停止应用、队列、调度器和 Reverb。
2. 切换到 `codex/archive-admin-ui-wip-20260823`。
3. 使用旧 Compose 配置重新启动。
4. 默认保留新增 AI 会话和审计数据。

### 数据恢复

- 数据库备份：`geoflow-old-database.dump`
- 文件备份：`geoflow-storage-app.tar.gz`
- 文件摘要：`geoflow-storage-app-files.sha256`
- Git 备份：`geoflow-repository.bundle`

恢复文件统一位于 `/Users/laoyao/AI Coding/01-Projects/OpenSource/GEOFlow-recovery/20260823`。

## 8. 观察期退出条件

48 小时内满足以下条件后执行最终清理：

- 无阻断级错误、数据减少或权限泄露。
- 队列、调度器、Reverb 和 Agent 恢复逻辑持续稳定。
- 站点设置、关键表和旧文件摘要保持一致。
- Runtime 分批开放得到明确确认。

观察通过后可删除旧 UI、临时回退开关、28080 Compose 候选环境和独立 `GEOFlow-UI-V3` 目录。删除前继续保留当前归档分支和恢复包。

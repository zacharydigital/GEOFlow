# GEOFlow AI 工作台系统知识库与图文回答升级方案

> 状态：最终评审版，待确认实施
> 版本：Final v2.0
> 日期：2026-08-28
> 目标页面：`/admin/ai-workspace`
> 关联页面：`/admin/knowledge-bases`
> 实施顺序：Phase 1 文本知识与站内导航；Phase 2 截图库与图文回答

## 1. 方案结论

推荐把 AI 工作台当前的 PHP 内置帮助目录升级为一套“系统知识库 + 可信站内导航 + 结构化媒体”的帮助体系。

Phase 1 新建一份专用于 AI 工作台的系统知识库，后台名称为「GEOFlow 后台功能与操作指南（AI 工作台专用）」。官方基线以一个 Markdown 文件和一份机器可读清单随代码发布。正文至少包含 10,000 个有效中文汉字，目标内容量为 20,000 至 30,000 个可见字符。系统通过显式、幂等的同步命令写入现有知识库，完成结构化切片和检索。独立的系统知识绑定表提供稳定键和数据库外键删除保护。授权管理员可以二次编辑，保存前完成路由指令校验，保存后记录修订并重建检索索引。编辑页和详情页持续提示：本知识库直接影响 AI 工作台回答。

AI 工作台收到问题后，只检索这份系统知识库，选取相关章节交给模型组织答案。当前界面语言、最近一轮用户问题和上一轮知识来源用于理解追问，历史助手回答不作为事实来源。模型负责语言表达，站内入口由服务端根据命名路由、当前后台前缀和管理员权限生成。回答按问题复杂度扩展前置条件、操作步骤、结果说明、常见问题和相关功能，同时保留现有 SSE 流式输出、会话历史、模型故障转移和安全净化。

Phase 2 建设专用截图素材层。截图与知识章节建立稳定关联，文件放在私有存储中，通过登录鉴权后的站内接口读取。媒体文件按内容哈希形成不可变版本，历史消息固定引用当时版本。服务端根据本轮命中的知识章节选择 0 至 3 张强相关截图，前端使用结构化字段渲染。模型输出中的原始图片地址和链接继续保持不可点击、不可直接渲染，避免权限绕过、外链注入和错误配图。

两个阶段均可独立合并。Phase 1 上线后已经具备完整的文字问答和站内导航能力；Phase 2 延后或取消时，Phase 1 仍可长期运行。

本轮最终评审补齐了数据库级删除保护、显式安装与升级流程、管理员编辑修订、跨语言回答、追问检索、上下文预算、索引版本一致性、媒体版本一致性、图库维护、可观测性和生产回滚边界。方案内已没有依赖实施人员临时决定的关键产品分支。

## 2. 当前状态与证据

### 2.1 当前 AI 工作台链路

当前 `/admin/ai-workspace` 已经具备稳定的产品外壳：

- 管理员鉴权、会话创建、重命名、归档与历史分页。
- SSE 流式状态、正文分片、停止生成、断线和错误处理。
- 已启用对话模型的就绪检测、超时、额度、故障转移和普通文本降级。
- Markdown 表格、列表、引用、代码块和复制能力。
- 服务端生成的相关功能卡片，最多展示 3 个可信站内入口。
- 会话消息持久化，并在 `meta` 中保存相关功能、推荐问题和生成性能。

当前请求链路为：

```text
用户问题
  -> AdminHelpKnowledgeCatalog 本地匹配
  -> 最多 5 条简短帮助事实
  -> AdminHelpAssistant 一次模型调用
  -> SSE 流式正文
  -> 服务端追加最多 3 个相关功能入口
  -> 保存回答与元数据
```

### 2.2 当前帮助知识的主要限制

`AdminHelpKnowledgeCatalog` 当前只有 17 条 PHP 数组条目。每条主要包含名称、短说明、关键词、3 个步骤、一个入口和两个追问。它适合基础导航，难以承载完整产品手册。

当前回答提示词限制为 1,200 tokens，并要求中文回答约 600 字。复杂流程容易缺少以下信息：

- 功能适用场景和目标结果。
- 依赖配置、角色权限和前置条件。
- 完整操作步骤、状态变化和结果检查。
- 系统内部逻辑、设计原则和能力边界。
- 常见失败原因、恢复方式和相关模块。
- 近期新增能力，如文章 AI 质检、标题生成运行、托管站点、Chrome 运营助手、PWA、Admin UI V3 和 CLI 0.2.0。

帮助目录内容写在代码中，后台知识库页面无法维护它。现有知识库中的官方 GEOFlow 资料也没有接入 AI 工作台。

### 2.3 当前知识库已经具备的基础能力

现有知识库支持：

- Markdown、文本和 Word 导入。
- 标题、章节路径、切片策略、来源和治理元数据。
- 结构化规则切片、可选语义规划和原文稳定重建。
- Embedding、pgvector、关键词、标题和治理权重组成的混合召回。
- 向量服务不可用时的本地回退检索。
- 异步暂存、批量向量化、原子切换和失败后保留旧切片。
- 后台 Markdown 编辑器、切片预览和手动刷新。

本地运行库中已经存在 15 个知识库，其中 4 个是 2026-08 形成的 GEOFlow 官方资料。它们主要服务产品介绍、部署运维、模型/RAG 和分发生态，当前 AI 工作台未读取这些资料。它们也可能已经被内容生成任务使用，因此本方案保留原资料，新增一份职责清晰的 AI 工作台系统知识库。

### 2.4 当前功能和路由规模

当前运行代码共注册 372 条路由，其中后台路由 289 条：

- 118 条包含 GET 页面或读取入口。
- 171 条包含 POST、PUT、PATCH 或 DELETE 状态变更。
- 后台主要覆盖数据分析、任务、文章、人工发布、内容资产、企业知识、URL 导入、分发、托管站点、网站设置、表单线索、AI 配置、用户权限、安全审计和系统更新。

知识库应以业务功能为章节组织，同时用路由覆盖审计确认所有后台能力都已归入某个功能章节、内部接口或受限操作。用户可点击的入口只选择稳定、只读、无需动态对象 ID 的后台 GET 页面。状态变更路由只用于核对操作覆盖，不直接输出为链接。

### 2.5 当前图片能力结论

现有普通知识库可以保存 Markdown 图片语法，但 AI 工作台无法安全显示知识图片：

- Markdown 渲染白名单没有 `img`。
- 模型输出中的 `a` 链接会转换为纯文本。
- SSE 与消息 `meta` 没有 `related_media` 字段。
- 普通知识库与知识切片没有稳定的截图关联实体。
- 知识库详情编辑器没有专用图片上传与管理流程。
- 现有图片库面向文章配图，文件通常位于公开路径，不适合承载可能包含后台界面的受保护截图。

Phase 2 需要增加一条受控的知识媒体链路。直接放开模型 Markdown 图片会扩大安全边界，也无法保证图片与问题相关。

## 3. 产品目标与验收标准

### 3.1 产品目标

1. AI 工作台能够准确解释 GEOFlow 每个主要功能的用途、逻辑、流程、亮点、原理和入口。
2. 管理员可以在后台知识库中查看和维护 AI 工作台的官方知识。
3. 系统知识库在所有受支持的 Web、API 和 CLI 删除路径中均不可删除。
4. 回答中的站内入口由服务端生成，能够适配自定义后台前缀和当前管理员权限。
5. 复杂问题获得更完整的操作指南，简单问题继续保持简洁。
6. Phase 2 能根据相关章节附带可信截图，图片在刷新历史会话后保持一致。

### 3.2 Phase 1 通过标准

| 项目 | 通过标准 |
|---|---|
| 官方 Markdown | 排除 Markdown 标记、代码围栏、URL 和空白后，正文不少于 10,000 个中文汉字；可见字符目标为 20,000 至 30,000 个 |
| 功能覆盖 | 当前 289 条后台命名路由和本方案新增路由全部归类为已说明功能、内部端点、动态子页或受限操作 |
| 核心模块 | 下文内容目录中的模块全部有独立章节和入口 |
| 检索召回 | 固定评测集 Top-3 章节召回率不低于 95% |
| 入口准确率 | 固定评测集中的服务端入口准确率 100% |
| 权限 | 普通管理员无法获得超级管理员专属入口 |
| 删除保护 | Web、API、CLI 和数据库外键均阻止删除系统知识库 |
| 编辑与恢复 | 授权管理员可编辑、查看修订并恢复；保存后索引异步更新，更新失败时旧索引继续服务 |
| 回答真实性 | 评测集中虚构路由、虚构功能和虚构执行结果均为 0 |
| 多语言 | `zh_CN`、`en`、`ja`、`es`、`ru`、`pt_BR` 均完成检索和回答冒烟；事实均来自同一官方基线 |
| 延迟与降级 | 本地高置信检索不调用 Embedding；Embedding 或切片服务失败时仍能使用就绪索引或随代码基线回答 |
| 性能 | 温缓存本地检索 P95 不高于 200 ms；接入知识检索后，排除模型和网络时间的服务端首片准备 P95 增量不高于 300 ms |
| 协议兼容 | 现有会话、SSE、停止生成和历史回放继续通过 |

### 3.3 Phase 2 通过标准

| 项目 | 通过标准 |
|---|---|
| 初始截图 | 首批 24 张经过脱敏和人工检查的中文后台截图 |
| 图片相关性 | 仅在命中同一功能章节且分数达到阈值时展示，单次 0 至 3 张 |
| 图片安全 | 必须登录后台；路径穿越、外链、SVG 和未知 MIME 被拒绝 |
| 历史一致性 | 新回答和历史回答固定展示同一媒体版本，替换截图不改变历史消息 |
| 可访问性 | 每张图片具有中文标题、alt 和简短说明 |
| 语言匹配 | 首批中文截图只在 `zh_CN` 界面展示，其他语言保持完整文字回答 |
| 媒体性能 | 缩略图按需加载；文字 SSE 不等待图片文件读取，图片故障不增加正文首片等待 |
| 响应式 | 320、390、768、1280、1440 宽度无横向溢出 |
| 无图降级 | 媒体表为空、文件丢失或 Phase 2 回滚时，文字回答完整可用 |

## 4. 范围与边界

### 4.1 本次范围

- 一份 AI 工作台专用系统知识库。
- 一份随代码发布的官方 Markdown 基线。
- 一份机器可读清单，保存系统键、语言、官方版本、文件、哈希和必需章节。
- 系统知识库绑定、版本、官方哈希、自定义状态和编辑修订。
- 后台列表、详情和编辑页面的系统知识提示与删除保护。
- AI 工作台的知识检索、证据上下文、来源章节和可信导航。
- 更完整的回答策略与输出额度。
- 路由覆盖、知识质量和问答评测。
- Phase 2 的截图采集、私有存储、版本管理、章节关联、服务端选择、后台图库管理和前端展示。

### 4.2 本次不开放

- AI 工作台执行创建、修改、删除、发布、升级或外部操作。
- AI 工作台读取任意客户知识库、任务数据、文章正文或线索个人信息。
- 模型直接生成可点击站内链接、外部链接或图片标签。
- 把后台 POST/PUT/PATCH/DELETE 路由暴露成可点击操作。
- 通过截图识别自动操作后台。
- Phase 2 自动持续抓取所有页面并在生产环境中更新图库。
- 自动覆盖管理员已经二次编辑的系统知识正文。
- 首版为 5 种非中文语言制作截图包。对应界面保持文字回答，后续可按同一媒体清单扩展。
- 从任意普通知识库自动升级或按名称认领系统知识库。

现有 GEOFlow 3.0 “后台帮助助手”产品边界保持不变。系统知识库负责解释和导航，执行型 Agent 仍由后续独立方案治理。

## 5. 推荐架构

```text
官方 Markdown 基线 + manifest.php
resources/knowledge/ai-workspace/
          |
          v
geoflow:sync-system-knowledge
  |  显式创建 / 安全升级 / 版本与哈希 / 提交索引
  v
system_knowledge_bases -> knowledge_bases -> knowledge_chunks
          ^                  |
          |                  +-> knowledge_base_revisions
          |
SystemKnowledgeBaseManager
  |  只读定位 / 健康状态 / 随代码基线回退
          |
          v
AdminHelpKnowledgeRetriever
  |  追问补全、本地/混合召回、章节去重、统一上下文预算
  +------------------------------+
  |                              |
  v                              v
AdminHelpAssistant        AdminHelpFeatureRegistry
  | 组织详细文字                 | 命名路由白名单、权限、图标
  +---------------+--------------+
                  v
       AdminHelpAnswerStream
       SSE delta + done metadata
                  |
                  v
       AI 工作台正文 + 章节来源 + 可信入口

Phase 2：
knowledge_media_assets + 私有不可变文件
          |
          v
AdminHelpMediaSelector -> related_media -> 前端安全图片组件
```

### 5.1 事实、导航和表现分层

系统采用三层职责：

- **事实层**：系统知识库 Markdown 和切片，保存功能说明、流程、原理、注意事项和常见问题。
- **导航层**：代码中的命名路由白名单，保存入口名称、路由名、图标、权限和稳定性。
- **表现层**：模型组织文字；服务端追加入口、来源和图片；前端只渲染经过验证的结构化字段。

管理员编辑 Markdown 可以改变回答事实和说明。管理员无法通过 Markdown 新增任意可点击 URL，也无法绕过路由权限。

#### 与 AI 工作台其他能力的集成边界

系统知识检索接入帮助回答和能力说明链路，不替代 AI 工作台已有或后续启用的意图、能力、审批和执行治理：

| 用户请求 | 主处理链路 | 系统知识库作用 |
|---|---|---|
| “任务怎么创建”“知识库有什么用” | 帮助回答 | 提供事实、步骤、原理、入口和截图 |
| “今天生成了多少文章”“渠道是否健康” | 已注册只读能力 | 提供指标口径和相关页面说明，实时结果仍来自能力处理器 |
| “启动任务”“同步设置”“发布内容” | 能力注册表、参数校验、审批和执行器 | 只解释前置条件与风险，不提供授权，也不改变执行结果 |

意图路由先决定本轮属于帮助、只读查询或执行流程。只有帮助和能力说明使用系统知识作为主要事实；能力处理器返回的实时业务数据保持最高优先级。知识正文无法新增 capability、改变风险等级、跳过审批或触发系统操作。

### 5.2 系统知识库的稳定标识与数据库保护

新增 `system_knowledge_bases` 绑定表，保持普通知识库表的通用语义：

| 字段 | 类型 | 用途 |
|---|---|---|
| `system_key` | string，主键 | 稳定定位；本项目使用 `ai_workspace_manual` |
| `knowledge_base_id` | unique foreign key | 指向现有知识库，使用 `RESTRICT ON DELETE` |
| `official_version` | string | 数据库当前采用的官方基线版本，如 `2026.08.28.1` |
| `official_content_hash` | char(64) | 判断正文是否仍等于上次采用的官方基线 |
| `customized_at` | nullable timestamp | 记录管理员是否做过二次编辑 |
| `last_synced_at` | nullable timestamp | 记录显式同步成功时间 |

`KnowledgeBase::systemBinding()` 提供关联，`isSystemManaged()` 以绑定是否存在判断。AI 工作台只按 `system_key` 查询绑定，不依赖数据库 ID、名称或排序。应用层返回友好错误，数据库外键负责最后一层删除保护。数据库高权限人员仍可先删除绑定，生产运维规范将这种操作列为受控数据变更。

### 5.3 创建、编辑和未来更新规则

`knowledge_base_revisions` 保存 `knowledge_base_id`、递增 `revision_number`、正文、内容哈希、来源类型 `official/manual/restore`、操作者、被恢复修订 ID 和创建时间。`(knowledge_base_id, revision_number)` 使用唯一约束。系统知识库名称、系统键和官方版本由清单与绑定管理，编辑页只开放正文；每次成功保存形成一条可恢复修订。

1. Schema 迁移只创建绑定表和修订表，不写入可编辑正文。
2. 新增显式幂等命令 `php artisan geoflow:sync-system-knowledge --key=ai_workspace_manual`。命令接入首次安装路径、系统更新计划的必需手工命令和部署文档，正常 Web 请求与常驻容器启动不写默认数据。
3. 系统键不存在时，命令创建新的知识库、绑定和初始修订。它不按名称认领已有知识库，避免把用户资料错误绑定成系统知识。
4. 系统键存在且当前正文哈希等于绑定表中的 `official_content_hash` 时，命令可以升级官方正文、版本和哈希，再提交切片同步。
5. 系统键存在且正文已经自定义时，命令保留管理员正文，只记录“官方更新可用”健康状态。详情页提供官方版本差异入口，首版不做自动合并。同步命令首版不提供强制覆盖自定义正文参数。
6. 授权管理员可以在差异页执行“采用当前官方版本”。该动作需要二次确认，先保存当前正文修订，再把随代码基线写成新修订，更新绑定版本与哈希并提交索引。手动编辑用于选择性吸收差异。
7. 授权管理员保存前执行字符数、必需章节和 route 指令校验。验证通过后写入新修订、设置 `customized_at`，再提交异步切片。
8. 恢复历史修订会创建一条新的修订记录，保留完整审计链。每个系统知识库保留最近 30 条修订，初始官方版本始终保留。
9. 切片采用现有暂存和原子切换机制。系统知识库同步使用 `requireRealEmbedding=false`，有 Embedding 时生成真实向量，服务不可用时仍能完成本地可检索索引。
10. 单次回答在开始时固定 `knowledge_base_id`、正文哈希、官方版本和 ready 切片来源哈希。编辑或索引切换发生在回答过程中时，本轮继续使用已固定的证据快照。
11. 索引尚未完成时，检索器对数据库正文建立缓存章节索引。数据库记录或绑定缺失时，使用随代码发布的官方基线只读回答并记录健康告警。GET 请求不负责重建数据，恢复动作由显式同步命令完成。

### 5.4 删除保护

删除保护覆盖三个层次：

- `KnowledgeBaseController::destroy()` 的后台 Web 删除。
- `MaterialLibraryService::deleteKnowledgeBase()` 的 API/CLI 共用删除。
- `system_knowledge_bases.knowledge_base_id` 的 `RESTRICT ON DELETE` 外键。

两处统一调用 `SystemKnowledgeBaseManager::assertDeletable()`。系统知识库返回明确的 409/表单错误。后台列表隐藏删除按钮并展示「AI 工作台系统知识」徽标。普通知识库继续沿用“被任务引用时不可删除”的现有逻辑。

批量删除、API、CLI 和未来新增删除入口都必须经过同一断言。架构测试扫描知识库删除调用，防止新增路径绕过。外键拦截关系型数据库中的直接知识库删除；随代码基线回退保证绑定损坏时仍有基础回答。

### 5.5 编辑权限与页面提示

- 所有管理员可以查看系统知识库详情和切片状态。
- 只有 `canManageProtectedWorkflows()` 通过的管理员可以修改系统知识库。
- 列表页显示系统徽标、用途和不可删除状态。
- 详情页顶部显示固定提示：本知识库用于 AI 工作台；修改会影响所有管理员的回答；保存后会重建索引；更新期间继续使用上一版索引。
- 编辑页保留现有 Vditor，系统标识和版本字段不可编辑。
- 保存前显示正文字符变化、route 指令变化和索引影响，确认后提交。
- 详情页显示当前正文哈希、官方版本、是否已自定义、索引来源和最近修订；授权管理员可预览差异并恢复修订。
- 所有编辑、恢复和显式同步操作写入管理员活动日志，只记录版本、哈希、字符数和操作者，不写完整正文。
- 普通知识库的权限和界面保持现状。

## 6. 官方 Markdown 内容设计

### 6.1 文件位置和命名

```text
resources/knowledge/ai-workspace/geoflow-admin-guide.zh_CN.md
resources/knowledge/ai-workspace/manifest.php
```

知识库后台名称：

```text
GEOFlow 后台功能与操作指南（AI 工作台专用）
```

系统键：

```text
ai_workspace_manual
```

`manifest.php` 固定保存 `system_key`、官方版本、权威语言、正文文件、SHA-256、必需章节、允许的 route 指令格式和字符门槛。构建门禁会重新计算哈希，文件内容和清单不一致时直接失败。中文 Markdown 是首版唯一事实基线。`en`、`ja`、`es`、`ru`、`pt_BR` 问答使用同一批证据，由模型按当前后台语言翻译表达；功能名、按钮名和入口名称优先取当前语言包，避免在非中文界面输出错误的中文控件名。

### 6.2 每个功能的统一说明维度

每个功能章节至少包含：

1. 功能定位。
2. 适用角色与典型场景。
3. 入口与相关入口。
4. 前置条件和依赖配置。
5. 页面结构与关键字段。
6. 标准操作流程。
7. 状态变化、异步任务和结果检查。
8. 底层逻辑与设计原则。
9. 产品亮点和与其他模块的联动。
10. 权限、安全和数据边界。
11. 常见问题、错误恢复和排查路径。
12. 推荐下一步。

正文字符门禁采用固定口径：移除代码围栏、内联代码、route 指令、URL、Markdown 标记和空白后，统计 Unicode Han 字符，结果必须不少于 10,000。总可见字符目标为 20,000 至 30,000 个。门禁同时检查每个一级模块至少有一篇完整章节，避免依靠重复内容满足长度。

### 6.3 站内入口指令

Markdown 使用可审计的路由指令，示例：

```markdown
功能入口：[[route:admin.tasks.index|进入任务管理]]
相关入口：[[route:admin.title-libraries.index|进入标题库]]
```

路由指令在检索上下文中保留入口名称，在最终输出阶段由服务端解析。解析规则：

- 路由必须存在于 `AdminHelpFeatureRegistry` 白名单。
- 路由必须在当前 Laravel 路由表中存在。
- 入口必须是稳定 GET 页面，不允许要求动态对象 ID。
- 入口必须通过当前管理员权限检查。
- URL 使用 `AdminWeb::routePath()` 生成，自动适配 `/admin`、`/geo_admin` 或子目录部署。
- 每次回答最多展示 3 个最相关入口。

`AdminHelpRouteCoverage` 将后台命名路由逐一归为 `entry`、`action`、`endpoint`、`dynamic` 或 `restricted`。只有 `entry` 可以进入可点击白名单。测试直接读取当前路由集合，任何新增且未分类的后台命名路由都会失败。该覆盖表用于审计完整性，官方 Markdown 按业务功能组织，无需逐条复述内部接口。

### 6.4 内容目录

官方 Markdown 首版覆盖以下内容：

#### A. 系统总览与快速开始

- GEOFlow 产品定位、适用场景和完整运行链路。
- 后台导航、最近访问、账号快捷入口和多语言。
- 三步上手：AI 配置、内容资产、任务创建。
- PWA 安装和独立窗口使用。
- AI 工作台自身的能力、边界和会话管理。

#### B. 数据中心

- 运营总览。
- 内容分析。
- 流量分析、访问日志、AI 爬虫和搜索蜘蛛识别。
- 线索分析。
- 分发分析。
- AI 可见性观测、来源和诊断。
- 时间范围、筛选、指标口径和数据为空时的判断。

#### C. 内容生产与任务

- 任务列表、新建、编辑、启停、批量启动和健康检查。
- 标题容量、循环策略、任务冲突和启动前 readiness。
- 模型、提示词、标题库、知识库、图片库、作者、分类和分发范围的关系。
- Scheduler、Queue、Worker、运行记录、失败恢复和幂等。
- 文章生成、草稿、审核、发布、回收站和批量操作。
- 文章编辑器、AI 辅助生成、图片上传和微信 HTML 导出。
- 风险扫描和文章 AI 质检：策略、分数、阻断、重检、人工放行和过期。
- Markdown 批量导出和下载令牌。

#### D. 内容资产

- 内容资产总入口。
- 分类管理。
- 作者管理。
- 关键词库：创建、导入、添加、批量删除和使用统计。
- 标题库：创建、导入、AI 批量生成、运行状态、重试、取消和去重。
- 图片库：上传、受管图片、路径安全、使用次数和文章插图。
- 普通知识库：导入、编辑、切片、Embedding、混合召回和任务关联。
- 企业知识库：项目、来源、解析、草稿、修订、校验、发布和恢复。
- URL 导入：创建、抓取、解析、预览、提交、状态和失败重试。

#### E. 分发与多站点

- 分发总览、渠道类型和适用场景。
- GEOFlow Agent、WordPress REST 和通用 HTTP API。
- 渠道创建、密钥、健康检查、前端能力、暂停和启用。
- 文章分发任务、失败重试、远端编辑和删除。
- 设置同步、单站预览、批量预览和执行。
- 目标站点包、静态输出、sitemap、TXT 地图、`llms.txt` 和 Schema。
- 托管站点：子域分配、生命周期、文章分配、发布配额、维护、索引、缓存和对账。
- 删除预检、影响范围、确认、取消和安全边界。

#### F. 人工发布与浏览器运营

- 人工发布工单、身份、账号、执行人、计划时间和状态流转。
- 发布结果、风险提示、重复提醒和 CSV 导出。
- Chrome 运营助手安装、设备码、管理员批准、领取、心跳、回执和撤销。
- 知乎草稿填充与最终人工确认边界。
- PWA 与浏览器扩展的职责关系。

#### G. 网站与获客

- 网站名称、品牌、Logo、基础展示和文章详情广告。
- 首页模块、排序、预设、导入和前台验证。
- 主题切换、主题复刻、隔离预览、迭代、审查包和归档。
- 线索表单创建、字段、启停和前台入口。
- 线索查看、状态更新、筛选、详情和导出。
- 前台首页、分类、文章、关于、归档、SEO、Open Graph、Schema 和 sitemap。

#### H. AI 配置

- AI 配置器总览。
- Provider、模型绑定和连接检测。
- Chat 与 Embedding 模型、默认模型、故障转移、额度和就绪状态。
- OpenAI 兼容接口、Gemini 原生接口和常见 URL 配置。
- 提示词、专用提示词、版本变化和文章质量影响。
- 知识切片策略、默认 Embedding、真实向量和 fallback。
- 模型检测失败、普通文本降级和 AI 工作台不可用排查。

#### I. 用户、安全与系统

- 个人资料、密码和浏览器客户端。
- 管理员创建、编辑、启停和权限范围。
- API Token、scope、撤销、幂等和版本协商。
- 安全设置、敏感词、操作日志和匿名遥测边界。
- 系统更新：检查、计划、预检、备份、执行、状态、重试和回滚。
- 受管图片、出站安全、SSRF 防护和只读安全审计。
- 队列、Horizon、Reverb、Redis、PostgreSQL/pgvector 和调度器。
- Docker 开发、生产部署、首次安装、升级和常见故障。

#### J. API、CLI 与扩展能力

- API v1 登录、scope、分页、错误结构和幂等。
- GEOFlow CLI 0.2.0 的配置、登录、目录、任务、运行、素材和文章操作。
- 浏览器运营协议。
- GEOFlow Agent Skill 的五种工作模式。
- 能力边界、隐私、密钥和高风险操作确认。

### 6.5 内容来源优先级

首版内容按以下证据优先级整理：

1. 当前运行路由、控制器、服务、请求校验和测试。
2. 当前页面、配置项、命令帮助和数据库状态。
3. `docs/CHANGELOG.md` 的 3.0.0 及最近版本更新。
4. AI 工作台、部署、浏览器运营、质量检查和分发运行手册。
5. README 和历史方案用于补充产品背景。

内容不复制密钥、真实账号、客户资料、线索个人信息或本机路径。

内容发布前还要执行隐私扫描、占位符扫描、重复段落检查和跨语言术语映射检查。示例统一使用虚构数据，不能保留待办或未决占位词、真实域名、真实邮箱、访问令牌或开发机绝对路径。

### 6.6 持续维护机制

- 官方 Markdown、清单、功能注册表和路由覆盖表由 GEOFlow 发布版本共同维护。
- 新增或修改后台页面、核心流程、权限、状态机、命令、API 或故障恢复方式时，代码评审必须填写“系统知识影响”。有影响时同步修改正文并提升官方版本；确认无影响时记录理由。
- 路由覆盖测试负责捕获新增、删除和改名路由。发布检查再核对 `docs/CHANGELOG.md`、运行手册和最近版本迁移，覆盖没有路由变化的业务逻辑更新。
- 每个官方版本保存内容哈希和必需章节。管理员已自定义正文时，详情页展示官方更新摘要与差异，管理员可以手动吸收更新或恢复官方版本。
- 每次正式发布至少重跑受影响模块的问题集；大版本发布重跑完整 72 条评测和六语言冒烟。

## 7. Phase 1：系统知识库、检索和详细回答

### 7.1 数据与基线

预计文件：

- `database/migrations/2026_08_28_120000_create_system_knowledge_base_tables.php`
- `resources/knowledge/ai-workspace/geoflow-admin-guide.zh_CN.md`
- `resources/knowledge/ai-workspace/manifest.php`
- `app/Models/SystemKnowledgeBase.php`
- `app/Models/KnowledgeBaseRevision.php`
- `app/Models/KnowledgeBase.php`
- `app/Services/AiWorkspace/SystemKnowledgeBaseManager.php`
- `app/Console/Commands/SyncSystemKnowledgeCommand.php`
- `app/Services/Admin/SystemUpdateManualCommandService.php`

工作内容：

1. 创建 `system_knowledge_bases` 绑定表和 `knowledge_base_revisions` 修订表，绑定外键使用 `RESTRICT ON DELETE`。
2. 通过清单加载官方 Markdown，创建 `ai_workspace_manual`，不按名称认领现有记录。
3. 写入 reviewed、low risk、system 类型来源元数据，以及官方版本、哈希和同步时间。
4. 执行中文汉字数、必需章节、route 指令和文件哈希门禁。
5. 显式同步命令发现缺失或落后索引时提交幂等切片任务，使用可降级的 Embedding 策略。
6. 为索引未完成、数据库记录缺失和切片失败提供只读基线回退。
7. `geoflow:install` 的首次空库路径调用同步服务；既有数据库由系统更新计划的 `required` 手工命令或运维命令显式调用。普通容器启动、迁移和 Web GET 请求不写入可编辑默认数据。

### 7.2 后台知识库体验

预计文件：

- `app/Http/Controllers/Admin/KnowledgeBaseController.php`
- `app/Services/GeoFlow/MaterialLibraryService.php`
- `resources/views/admin/knowledge-bases/index.blade.php`
- `resources/views/admin/knowledge-bases/detail.blade.php`
- `resources/views/admin/knowledge-bases/form.blade.php`
- `lang/*/admin.php`

工作内容：

1. 列表返回系统绑定、系统徽标、健康状态和删除能力。
2. 系统知识库不渲染删除表单。
3. Web 与素材服务统一拒绝删除。
4. 详情和编辑页增加用途、影响范围、索引状态和官方版本提示。
5. 系统知识库编辑要求受保护工作流权限。
6. 保存前校验正文和 route 指令，保存后写入修订并沿用现有异步切片、原子切换和质量失效机制。
7. 系统知识库的手动刷新改为“重建检索索引”，允许 Embedding 故障时回退；普通知识库现有强制真实向量按钮保持原逻辑。
8. 页面展示 `healthy`、`indexing`、`fallback`、`failed`、`customized` 和 `official_update_available` 健康状态，并给出可执行的恢复说明。

### 7.3 功能入口注册表

预计文件：

- `app/Services/AiWorkspace/AdminHelpFeatureRegistry.php`
- `app/Services/AiWorkspace/AdminHelpRouteCoverage.php`
- `app/Services/AiWorkspace/AdminHelpKnowledgeCatalog.php`（缩减或兼容代理）

注册表只保存可信导航信息：

- 稳定 feature ID。
- 页面翻译键与图标。
- 命名路由。
- 权限级别。
- 可检索别名。
- 是否用于首页快捷提问。

长说明、完整步骤和原理迁入系统知识库。现有首页快捷入口和建议问题可以继续从注册表读取，避免首屏依赖模型。

路由覆盖表为每条后台命名路由记录分类和所属 feature ID。`entry` 类路由必须是无动态对象参数的稳定 GET 页面；`action`、`endpoint`、`dynamic` 和 `restricted` 只参与覆盖审计。新路由未分类、旧路由消失或 route 指令指向非 `entry` 时，自动化测试失败。

### 7.4 知识检索器

预计文件：

- `app/Services/AiWorkspace/AdminHelpKnowledgeRetriever.php`
- `app/Services/AiWorkspace/AdminHelpQueryContextResolver.php`
- `app/Services/GeoFlow/KnowledgeRetrievalService.php`（仅增加兼容的检索选项时修改）

每轮检索规则：

1. 按 `system_key` 获取系统知识库。
2. 优先使用 ready 切片；切片缺失时使用缓存后的 Markdown 章节索引。
3. 召回最多 16 个候选，按相关性、标题、章节和审核状态排序。
4. 去除重复章节，最终提供最多 8 个证据块。
5. 本轮总输入预算为 24,000 个字符：知识证据最多 10,000 个，历史最多 10,000 个，系统指令、当前问题和结构化元数据预留至少 4,000 个。超限时先裁剪旧历史，再按低分到高分裁剪证据。
6. 提取证据中的 route 指令，交给注册表做权限过滤。
7. 保存命中知识库版本、正文哈希、切片来源哈希、章节路径、chunk ID、检索模式和分数摘要。
8. 无直接命中时只提供总览和澄清建议，避免把所有系统知识塞入模型上下文。

为降低首轮延迟，短问题先执行本地标题、别名和关键词召回。存在精确 feature/route 别名命中，或本地最高归一化分数不低于 0.42 且领先第二名至少 0.08 时，直接采用本地结果。其余问题再启用现有向量混合召回。检索结果按“正文哈希 + 切片来源哈希 + 当前语言 + 权限范围 + 问题规范化文本”短时缓存，编辑和索引切换后自然失效。

追问解析只使用当前问题、上一条用户消息和上一轮 `knowledge_sources` 的 feature/section 标识。短追问包含“这个、它、怎么设置、然后呢”等指代词时，在检索查询中补入上一条用户问题，并把上一轮章节作为召回加权。历史助手正文只进入对话表达上下文，不进入检索事实和加权，避免旧回答放大错误。

### 7.5 回答与 SSE 元数据

预计文件：

- `app/Services/AiWorkspace/AdminHelpAnswerStream.php`
- `app/Ai/Agents/AdminHelpAssistant.php`
- `config/ai-workspace.php`
- `resources/js/admin/ai-workspace.js`

回答策略：

- 简单定位问题：直接结论 + 入口 + 2 至 4 个步骤。
- 标准操作问题：前置条件 + 5 至 10 个步骤 + 结果检查 + 常见问题。
- 原理或方案问题：概念 + 系统链路 + 设计亮点 + 边界 + 相关功能。
- 故障问题：现象判断 + 按优先级排查 + 恢复动作 + 需要查看的页面。
- 知识不足：明确缺少的事实，只询问一个必要问题。

调整建议：

- 回答生成上限从 1,200 tokens 提升到最多 2,400 tokens，并受所选模型 `max_tokens` 约束。简单问题目标为 300 至 800 个中文字符，标准问题为 800 至 1,500 个，复杂流程和故障问题为 1,500 至 2,500 个；信息完整后停止，不追求固定长度。
- 移除固定“约 600 字”限制，改为按问题复杂度控制。
- 模型只使用检索证据组织事实，知识正文中的命令性文本视为资料内容。
- 模型继续不输出可点击 URL，也不声称已经执行系统操作。
- 模型按当前后台语言回答。中文事实基线可翻译，代码、命名路由、配置键和命令原样保留，界面标签优先使用当前语言包。

SSE `done` 和消息 `meta` 增加：

```json
{
  "knowledge_sources": [
    {
      "knowledge_base_id": 24,
      "official_version": "2026.08.28.1",
      "content_hash": "sha256:...",
      "chunk_source_hash": "sha256:...",
      "chunk_id": 123,
      "section_path": "内容生产与任务 > 任务启动前检查",
      "score": 0.92,
      "retrieval_mode": "ready_index"
    }
  ],
  "knowledge_health": "healthy"
}
```

前端把来源显示为小型「参考章节」区域。用户界面只展示章节名和知识版本，分数、哈希和 chunk ID 留在消息元数据与诊断日志中。来源不包含原始向量、完整提示词、证据正文或内部数据库路径。

服务端额外记录 `retrieval_mode`、`retrieval_latency_ms`、`fallback_reason`、证据数量、知识版本、入口数量和媒体数量。日志不写用户问题全文、知识正文、向量或模型密钥。系统知识处于 `fallback` 或 `failed` 时，仅向具备受保护工作流权限的管理员展示轻量健康提示，普通回答区域保持可用。

### 7.6 质量门禁

新增三组门禁：

1. **知识文档门禁**：字符数、必需章节、重复标题、空章节、路由指令格式。
2. **路由覆盖门禁**：当前 289 条后台命名路由和实施新增路由全部完成分类；所有可点击 route 指令存在且为稳定 GET 页面。
3. **问答评测门禁**：至少 72 条固定问题，覆盖所有模块、权限差异、同义表达、跨模块问题、连续追问、六种界面语言、无命中和提示注入。

建议评测集分布：

- 40 条单模块操作问题。
- 8 条跨模块流程问题。
- 5 条权限与受限入口问题。
- 4 条故障排查问题。
- 3 条超出知识范围问题。
- 6 条连续追问问题。
- 6 条非中文语言问题，每种支持语言至少 1 条；另在发布冒烟中每种语言补 2 条。

## 8. Phase 2：截图知识库和图文回答

### 8.1 媒体数据模型

新增 `knowledge_media_assets`：

| 字段 | 用途 |
|---|---|
| `id` | 媒体 ID |
| `knowledge_base_id` | 关联系统知识库 |
| `asset_key` | 逻辑稳定键，如 `tasks.create.form` |
| `asset_version` | 同一逻辑截图的递增版本 |
| `supersedes_id` | 指向被替换的旧媒体记录 |
| `section_key` | 对应知识章节稳定键 |
| `route_name` | 对应可信后台页面 |
| `title` | 图片标题 |
| `alt_text` | 无障碍文本 |
| `caption` | 回答下方说明 |
| `keywords_json` | 检索别名 |
| `storage_path` | 私有文件路径 |
| `thumbnail_path` | 私有缩略图路径 |
| `mime_type` | 仅允许 PNG、WebP |
| `width` / `height` | 原图尺寸 |
| `content_hash` | 去重和完整性检查 |
| `locale` | 首版 `zh_CN` |
| `official_version` | 对应知识官方版本 |
| `captured_at` / `captured_app_version` | 截图时间与应用版本 |
| `sort_order` / `is_active` | 展示控制 |
| `created_by_admin_id` | 系统导入或后台维护操作者 |

源文件和清单随代码发布：

```text
resources/knowledge/ai-workspace/media/
resources/knowledge/ai-workspace/media/manifest.json
```

运行时复制到：

```text
storage/app/private/ai-workspace-knowledge-media/
```

文件名使用内容哈希，写入后不覆盖。同一 `asset_key` 更新截图时创建新记录并停用旧记录；旧记录和旧文件继续供历史消息读取。数据库唯一约束覆盖 `(knowledge_base_id, asset_key, locale, asset_version)`，`content_hash` 用于去重。消息元数据保存媒体 ID、版本、哈希、标题、alt 和说明快照，读取接口校验数据库记录与文件完整性。后续修改媒体说明不会改变已保存消息。

知识库详情页新增「配图素材」页签。受保护工作流管理员可以查看、导入、编辑标题/alt/说明/关键词/章节关联、调整排序、启用和停用。界面不提供物理删除；替换动作始终创建不可变新版本。导入只接受 PNG 和 WebP，单图最大 8 MB、最长边最大 4096 像素，服务端核验真实 MIME、像素、哈希和 route 白名单。首批官方截图统一转成 WebP，最长边不超过 1600 像素，单图目标小于 500 KB，24 张总量控制在 12 MB 内。

### 8.2 首批 24 张截图清单

首批覆盖：

1. AI 工作台起始页。
2. AI 工作台对话与相关功能。
3. 数据中心总览。
4. AI 可见性。
5. 任务列表。
6. 任务创建基础配置。
7. 任务内容资产与分发配置。
8. 文章列表与状态筛选。
9. 文章编辑器。
10. 文章 AI 质检详情。
11. 内容资产总览。
12. 标题库 AI 生成。
13. 知识库列表。
14. 知识库详情与切片。
15. 企业知识库项目。
16. URL 导入预览。
17. 分发渠道列表。
18. 分发渠道详情与健康状态。
19. 托管站点详情。
20. 人工发布列表与状态。
21. Chrome 运营助手配对页。
22. AI 模型配置与检测状态。
23. 网站首页模块与主题。
24. 系统更新计划与备份状态。

截图统一使用隔离的测试浏览器、固定安全样例数据和可复现的采集清单。采集前清理 API Key、Token、邮箱、手机号、线索内容、真实域名、客户名称和本机路径。每张图片完成机器隐私扫描和双人视觉检查，记录采集应用版本、路由、语言、视口和内容哈希。截图清单指向的路由消失、应用主次版本变化或页面结构测试失败时，将该素材标记为待复核；待复核素材不会自动进入新回答。

### 8.3 媒体选择规则

`AdminHelpMediaSelector` 只根据服务端检索结果选图：

1. 优先匹配命中 chunk 的 `section_key`。
2. 其次匹配 route name 和功能 ID。
3. 再用标题与关键词做补充评分。
4. 分数未达到阈值时返回空数组。
5. 同一回答最多 3 张，默认 1 张。
6. 同一页面的多张截图按知识步骤顺序展示。
7. 当前管理员无权访问对应页面时不展示截图。
8. 文件缺失、哈希不符或 MIME 不合法时跳过并记录健康告警。
9. 媒体语言必须与当前后台语言一致。首批只有 `zh_CN`，其他语言返回空数组。
10. 每轮开始时固定媒体 ID、版本和哈希，回答期间图库更新不改变本轮选择。

### 8.4 私有图片接口

新增登录鉴权路由：

```text
GET /admin/ai-workspace/media/{mediaAsset}
```

接口要求：

- 复用后台登录和权限中间件。
- 只接受数据库记录中的私有相对路径。
- 校验系统知识绑定、route 权限、文件哈希和 MIME。媒体记录不存在或文件已按留存规则清理时返回 404。
- `is_active` 只控制新回答选择；历史消息可以读取其固定引用的已停用旧版本。
- 返回 `Content-Disposition: inline`、基于内容哈希的 ETag、私有缓存头、`nosniff` 和限制型图片响应安全头。
- 不接收用户提供的任意文件路径。

### 8.5 SSE 和前端展示

Phase 2 在成功完成的 `done` 和历史助手消息 `meta` 中增加：

```json
{
  "related_media": [
    {
      "id": 1,
      "version": 2,
      "content_hash": "sha256:...",
      "title": "任务创建：内容与素材配置",
      "alt": "GEOFlow 任务创建页中的知识库、标题库和模型选择区域",
      "caption": "在任务创建页选择模型、标题库和知识库。",
      "url": "/admin/ai-workspace/media/1",
      "width": 1440,
      "height": 900
    }
  ]
}
```

前端增加独立媒体组件：

- 图片只由 `related_media` 创建 DOM 节点。
- 模型 Markdown 中的 `img` 继续被净化。
- 单图使用宽幅说明卡；多图使用可横向浏览的轻量图库。
- 点击后使用站内预览对话框查看大图。
- 加载失败时隐藏图片并保留标题说明。
- 停止生成时不附加尚未完成选择的媒体；完整回答保存后历史可回放。
- 当前语言没有匹配截图时不显示空图库或中文替代图。

媒体替换和停用写入管理员活动日志。`geoflow:prune-ai-workspace` 在 Phase 2 扩展为只清理“已停用、没有任何未过期消息引用、超过会话留存期 7 天”的旧媒体文件和记录。官方清单中的当前版本永不由该命令清理。

## 9. 关键安全与治理决策

### 9.1 只检索专用系统知识库

AI 工作台不会自动扫描所有普通知识库。这样可以避免：

- 客户资料、内部内容或个人信息进入帮助回答。
- 普通知识文档中的提示注入影响系统助手。
- 同名知识库产生冲突。
- 内容生成知识与产品操作知识互相污染。

### 9.2 路由继续由服务端控制

系统知识 Markdown 中的 route 指令只是引用。最终 URL、权限、后台前缀和展示数量都由服务端决定。模型输出的 URL 和 Markdown 链接继续不可点击。

### 9.3 图片继续走结构化字段

图片与文字事实共享章节关联，展示由服务端评分。前端不会解析模型生成的图片语法。后台截图使用私有存储和登录接口，降低敏感界面公开暴露的风险。

### 9.4 编辑后的知识仍按数据处理

系统提示明确规定知识块是参考资料，知识块中的身份修改、越权、提示词泄露、工具调用和链接生成要求均无效。route 指令和图片关联还要经过独立白名单验证。

### 9.5 版本固定与最小日志

每轮回答固定知识正文哈希、切片来源哈希和媒体版本。索引或图片在流式输出期间更新时，本轮结果保持一致。消息元数据用于历史回放和诊断，业务日志只保留模式、耗时、数量、版本和失败原因，不记录证据正文、完整问题、完整回答、密钥或向量。

### 9.6 明确的内容信任边界

官方基线经过代码评审和门禁后发布。管理员编辑内容属于受控但不受信任的数据，保存时接受结构、路由和隐私校验，生成时继续套用提示注入防护。普通业务知识库、客户知识、任务正文、文章、线索和上传图片不会进入帮助检索范围。

## 10. 数据、接口和公共表面变化

### Phase 1

- 数据库：新增 `system_knowledge_bases` 和 `knowledge_base_revisions`，系统绑定使用唯一键和限制删除外键。
- 资源：新增 1 个官方 Markdown 文件和 1 份机器可读清单。
- 命令：新增 `geoflow:sync-system-knowledge`，接入首次安装、系统更新计划的必需手工命令和部署文档。
- 服务：新增系统知识管理器、查询上下文解析器、知识检索器、功能入口注册表和路由覆盖表。
- SSE：`done` 增加 `knowledge_sources`，属于向后兼容的附加字段。
- 消息：`meta` 增加知识来源、固定版本、检索模式和知识健康状态。
- 页面：知识库列表、详情、编辑增加系统提示、健康状态、修订恢复和删除状态。
- 环境变量：无新增。
- 外部服务：无新增；继续复用现有对话模型和可选 Embedding 模型。

### Phase 2

- 数据库：新增 `knowledge_media_assets`。
- 资源：新增 24 张首批截图和 1 个媒体清单。
- 路由：新增 1 个后台鉴权图片读取路由，以及受保护的媒体导入、编辑、替换、启用和停用路由。
- SSE：增加 `related_media`，属于向后兼容的附加字段。
- 前端：新增知识库图库管理、安全媒体渲染和预览组件。
- 留存：扩展现有 AI 工作台清理命令，只清理超过会话引用窗口的停用旧媒体。
- 环境变量：无新增；没有媒体时自然降级为文字回答。
- 外部服务：无新增。

预计会涉及 20 个以上文件，跨数据、后台、AI 服务、前端、语言、测试和文档。实施时按阶段拆分提交和验证，避免与当前工作树中的其他改动混合。

## 11. 测试与验收

### 11.1 Phase 1 自动化测试

新增或扩展：

- `tests/Feature/AdminAiWorkspaceTest.php`
- `tests/Feature/AiWorkspaceRuntimeProtocolV2Test.php`
- `tests/Feature/AdminMaterialsPagesTest.php`
- `tests/Feature/ApiV1ContractTest.php`
- `tests/Feature/SystemKnowledgeBaseTest.php`
- `tests/Feature/SyncSystemKnowledgeCommandTest.php`
- `tests/Unit/AiWorkspace/AdminHelpKnowledgeRetrieverTest.php`
- `tests/Unit/AiWorkspace/AdminHelpQueryContextResolverTest.php`
- `tests/Unit/AiWorkspace/AdminHelpFeatureRegistryTest.php`
- `tests/Unit/AiWorkspace/AdminHelpRouteCoverageTest.php`
- `tests/JavaScript/ai-workspace.test.js`

覆盖场景：

- Schema 迁移不写正文，首次安装或显式同步创建唯一系统知识库。
- 同步命令重复执行不产生重复记录；未自定义内容安全升级，已自定义内容保留并提示官方更新。
- “采用当前官方版本”先保存原正文修订，再更新绑定与索引；同步命令没有强制覆盖自定义正文入口。
- 自定义后台前缀下的相对入口正确。
- Web 删除、API 删除、CLI 删除和直接模型删除均被应用断言或数据库外键阻止。
- 普通知识库仍可按原规则删除。
- 受保护权限控制系统知识编辑。
- 非法 route 指令、缺失必需章节和过短正文无法保存。
- 编辑、恢复和官方同步产生修订与活动日志；恢复会创建新修订。
- 编辑后发起索引，新索引失败时旧切片仍可检索。
- 系统知识重建在 Embedding 不可用时仍生成可用本地索引。
- 缺失系统记录和缺失切片时官方基线回退可用。
- route 指令白名单、动态参数、外链和权限过滤。
- 当前后台每条命名路由恰好完成一种分类，新增未分类路由使测试失败。
- 复杂问题命中多个相关章节并生成详细回答上下文。
- 连续追问使用上一条用户消息和上一轮章节加权，不把助手正文作为检索事实。
- 24,000 字符总预算、证据裁剪顺序和模型 `max_tokens` 上限生效。
- 回答期间发生正文编辑或索引切换时，本轮知识版本保持固定。
- 六种后台语言均可使用同一事实基线回答，界面标签使用当前语言包。
- SSE 和历史消息持久化 `knowledge_sources`。
- 日志和消息诊断字段不包含证据正文、完整提示词、密钥或向量。
- 旧客户端忽略附加字段后继续工作。

### 11.2 Phase 2 自动化测试

新增或扩展：

- `tests/Feature/AiWorkspaceKnowledgeMediaTest.php`
- `tests/Feature/SystemKnowledgeMediaManagementTest.php`
- `tests/Unit/AiWorkspace/AdminHelpMediaSelectorTest.php`
- `tests/JavaScript/ai-workspace.test.js`
- 浏览器 AI 工作台 smoke 测试。

覆盖场景：

- 私有媒体必须登录。
- 不同管理员权限过滤。
- 文件路径、MIME、哈希和启用状态校验。
- 导入、编辑元数据、替换、启用和停用要求受保护权限并写活动日志。
- 替换创建不可变新版本，历史消息继续读取旧媒体 ID、版本和哈希。
- 强相关命中展示 1 至 3 张图。
- 弱相关和无命中不展示图。
- 中文截图只在 `zh_CN` 展示，其他语言完整降级到文字。
- SSE 完成时持久化媒体；错误和停止不附加媒体；历史回放固定版本。
- 停用旧媒体只有在没有未过期消息引用且超过宽限期后才可清理。
- Markdown 中的任意 `img`、外链和 data URL 继续被净化。
- 320 至 1440 宽度布局、键盘和屏幕阅读器标签。

### 11.3 建议验证命令

```bash
php artisan test --compact tests/Feature/AdminAiWorkspaceTest.php tests/Feature/AiWorkspaceRuntimeProtocolV2Test.php
php artisan test --compact tests/Feature/AdminMaterialsPagesTest.php tests/Feature/ApiV1ContractTest.php
php artisan test --compact tests/Feature/SystemKnowledgeBaseTest.php tests/Feature/SyncSystemKnowledgeCommandTest.php tests/Unit/AiWorkspace
node --test tests/JavaScript/ai-workspace.test.js
php artisan geoflow:sync-system-knowledge --key=ai_workspace_manual
php artisan route:list --path=admin/ai-workspace --except-vendor
php artisan route:list --path=admin/knowledge-bases --except-vendor
vendor/bin/pint --dirty --format agent
npm run build
git diff --check
```

Phase 2 浏览器验收使用隔离测试浏览器，避免改变用户可见浏览器窗口尺寸。

正式发布门禁还包括 72 条问答评测、24 张截图清单校验、官方 Markdown 隐私与占位符扫描、六种语言冒烟、数据库外键测试和 `git diff --check`。评测原始问题使用虚构数据，评测报告只记录必要摘要。

## 12. 发布、回滚和故障处理

### 12.1 Phase 1 发布

1. 备份数据库。
2. 部署代码和官方 Markdown。
3. 执行迁移。
4. 执行 `php artisan geoflow:sync-system-knowledge --key=ai_workspace_manual`。该步骤写入系统知识并提交幂等索引任务。
5. 构建前端资源。
6. 确认 knowledge 队列正在运行。
7. 检查命令退出码、系统知识库徽标、数据库删除保护、健康状态、切片 ready 状态和问答评测。

首次空库安装通过 `geoflow:install` 调用同一同步服务。现有站点升级由 `SystemUpdateManualCommandService` 在 `manual_commands` 中把该步骤标为 `required`；手工部署文档也列出同一命令。任何入口均遵守“自定义正文不自动覆盖”规则。

### 12.2 Phase 1 回滚

- 恢复旧的帮助目录和回答链路。
- 保留系统知识库、绑定、修订、正文和切片，不执行数据删除，也不执行生产 `migrate:rollback`。
- 新表和消息 `meta` 附加字段可以留存，旧代码会忽略。
- 新索引失败时继续使用上一版 ready 切片或官方基线回退。
- 回滚后需要再次上线新版时，显式同步命令会根据绑定版本和正文哈希幂等续跑。

### 12.3 Phase 2 发布

1. 执行媒体表迁移。
2. 执行 `php artisan geoflow:sync-system-knowledge --key=ai_workspace_manual --media`，导入媒体清单和 24 张私有截图。
3. 校验哈希、MIME、尺寸、route 和权限。
4. 构建前端资源。
5. 先验证文字回答，再验证有图和无图问题。

### 12.4 Phase 2 回滚

- 停止返回 `related_media` 并恢复旧前端资源。
- 保留媒体表、版本记录和私有文件，不执行生产迁移回滚。
- 旧客户端会忽略 `related_media`。
- 图片故障不会阻断正文 SSE、消息保存或相关功能入口。

## 13. 风险与应对

| 风险 | 应对 |
|---|---|
| 管理员编辑导致官方更新冲突 | 使用官方哈希和 `customized_at`；保留本地正文、记录修订并提示新版本 |
| 升级或容器启动覆盖用户正文 | Schema 迁移只建表；显式同步命令仅更新未自定义正文；普通启动和 GET 请求不写正文 |
| 切片更新失败 | 暂存和原子切换；继续使用上一版 ready 切片 |
| 回答期间索引切换 | 回答开始时固定正文和切片来源哈希，本轮始终使用同一证据快照 |
| Embedding 服务变慢 | 高置信本地召回直接返回，低置信度再调用混合召回；同步允许本地索引回退 |
| 长知识和长历史挤占模型上下文 | 24,000 字符统一预算，预留系统与问题空间，先裁剪旧历史和低分证据 |
| 知识正文新增恶意指令 | 知识块按资料处理；路由和媒体独立白名单验证 |
| 可点击入口越权 | route 存在性、稳定 GET、管理员权限三重检查 |
| 截图泄露敏感信息 | 安全样例数据、人工脱敏检查、私有存储、登录读取 |
| 图片与问题相关性差 | 章节强关联、阈值、最多 3 张、低分返回空 |
| 替换截图改变历史回答 | 文件和媒体记录不可变，消息固定媒体 ID、版本和哈希 |
| 非中文界面显示中文截图 | 媒体按 locale 精确匹配，首版非中文界面使用完整文字降级 |
| 系统知识库被误删 | UI 隐藏、Web/API/CLI 统一拦截、数据库限制删除外键和官方基线回退 |
| 未来功能新增后文档过期 | 路由覆盖门禁、更新日志核对、系统版本和官方哈希 |
| 日志扩大敏感数据面 | 只记录版本、模式、耗时、数量、哈希和失败原因，不记录证据正文与完整问答 |

## 14. 最脆弱假设

本方案假设 AI 工作台在本轮继续承担“系统帮助与导航”职责。未来如果重新开放创建、修改、发布等执行型能力，系统知识库仍可作为说明和检索来源，执行权限、参数 Schema、审批、幂等和审计必须继续由独立能力注册表和工作流引擎管理。

本方案也假设管理员二次编辑需要长期保留。未来官方版本升级会尊重 `customized_at`，由管理员决定何时吸收新版内容。产品如果要求官方内容强制同步，需要新增差异预览和合并界面。

## 15. 备选方案与取舍

### 备选 A：继续扩展 PHP 帮助目录

实现成本较低，仍然缺少后台编辑、长文知识管理、切片、版本和媒体关联。内容规模增长后维护成本会快速上升。本方案不采用。

### 备选 B：让 AI 工作台搜索所有知识库

检索范围更大，同时会引入客户资料、提示注入、同名冲突、权限和回答污染。本方案只搜索专用系统知识库。

### 备选 C：允许模型直接输出链接和 Markdown 图片

前端改动较少，权限、后台前缀、外链注入、图片相关性和历史稳定性都难以保证。本方案继续使用服务端结构化入口和媒体。

### 最小可交付版本

只实施 Phase 1。它已经满足系统知识库、不可删除、可编辑、详细问答和站内导航的核心目标。Phase 2 作为独立增强，在文字质量和检索准确率稳定后上线。

## 16. 实施顺序与确认点

### Phase 1

1. 编写官方 Markdown、机器清单、功能注册表和全量路由覆盖表。
2. 建立系统绑定、数据库删除保护、修订表和显式同步命令。
3. 完成不可删除、受控编辑、差异提示、修订恢复和知识健康状态。
4. 接入追问解析、系统知识检索、上下文预算、版本固定和可信 route 指令。
5. 调整详细回答与多语言策略，持久化精简知识来源和诊断字段。
6. 完成 72 条评测、六种语言冒烟、自动化测试和隔离浏览器验收。

Phase 1 验收通过后，系统已具备完整文字帮助能力，可以独立发布。

### Phase 2

1. 建立知识媒体表、私有存储和媒体清单。
2. 使用隔离测试浏览器采集并脱敏 24 张截图。
3. 完成不可变版本、章节、route、locale 和应用版本关联。
4. 增加后台图库管理、服务端媒体选择、鉴权读取和 SSE 字段。
5. 增加前端图库、预览、历史固定回放、留存清理和响应式验证。
6. 完成相关性、安全、隐私、语言匹配、无图降级和浏览器验收。

## 17. 最终评审结论

本方案已经锁定以下实施决策：

- 新建专用系统知识库，现有 4 个官方 GEOFlow 内容知识库保持原职责。
- 系统知识库全员可查看，受保护工作流管理员可编辑和恢复修订；应用入口与数据库外键共同阻止删除。
- 官方 Markdown 与机器清单随代码发布，数据库正文用于运行和二次编辑，显式同步命令负责创建与安全升级。
- AI 工作台只检索该系统知识库。
- 站内链接使用 route 指令、全量路由分类和服务端权限白名单。
- 问答支持连续追问和六种后台语言，单轮采用 24,000 字符输入预算，回答上限为 2,400 tokens 并服从模型配置。
- 每轮固定知识正文、切片和媒体版本，保证流式回答与历史回放一致。
- Phase 2 首批制作 24 张中文后台截图，使用私有不可变存储、后台图库管理和结构化输出；其他语言首版使用完整文字回答。
- 两阶段分别验收和发布。

最终评审确认方案覆盖产品、数据、权限、安全、检索、生成、媒体、国际化、审计、留存、发布和回滚。获得实施确认后开始 Phase 1；Phase 1 完成并验收后提交 Phase 2 开始确认。

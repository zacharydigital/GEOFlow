# GEOFlow 新旧系统融合最终实施方案

> 状态：Final v2.0，待用户确认后实施
>
> 日期：2026-08-23
>
> 最终代码库：`GEOFlow`
>
> 最终管理后台：`http://localhost:18080/admin`
>
> 数据基准：当前 `GEOFlow` 数据库、站点设置、前台模板与文件
>
> 界面基准：`GEOFlow-UI-V3` 的真实管理页面与 AI 工作台

## 1. 最终结论

本次融合采用“保留 GEOFlow 业务与数据，迁入 UI V3 和 AI 工作台”的路线。

完成后只保留以下一套系统：

- 一个代码目录和代码库：`GEOFlow`。
- 一个管理后台入口：`http://localhost:18080/admin`。
- 一套 PostgreSQL 数据：沿用当前 18080 系统的数据。
- 一套 Redis、队列、调度和实时事件服务。
- 一套前台模板与站点设置：沿用当前 18080 系统的主题、首页模块、内容和测试数据。
- 一套管理界面：采用 UI V3 公共壳层和页面样式。
- 一个真实 AI 工作台：保留 UI V3 交互，接入受控 Agent、审批、队列、审计和恢复链路。

`http://localhost:28080` 只用于融合期间的候选版本验收。完成切换和观察后，删除 UI V3 的独立容器、网络、数据库卷和源码目录。

## 2. 本轮 Review 结论

原方案的主方向正确，已经覆盖界面迁移、旧数据保留、AI 工作台接入和最终单系统收口。本轮补齐了会直接影响实施安全与验收结果的十二项内容：

1. 增加双工作树保护，先固化两边未提交内容，再进入干净集成工作树。
2. 明确 UI V3 分支是当前 GEOFlow 基线的直接后续提交，可以保留连续 Git 历史，减少人工拷贝。
3. 增加旧数据库逻辑备份与克隆验证，禁止复制正在运行的 PostgreSQL 数据目录。
4. 增加 `APP_KEY`、加密模型密钥、Session、站点域名和文件存储的保留规则。
5. 增加最终容器拓扑，补齐 Nginx 静态资源网关、两条 AI 队列、Scheduler 和 Reverb。
6. 增加资源缓存规则，Vite 指纹资源长期缓存，动态页面禁用缓存，主题与上传资源采用短缓存。
7. 增加数据库变更清单、迁移验证和前向修复原则，避免错误回滚共享会话表。
8. 增加 AI 模型结构化输出检测、运行时灰度、外部操作沙箱和 `outcome_unknown` 对账门禁。
9. 增加切换前的队列排空、调度暂停、维护窗口和服务恢复顺序。
10. 增加输入框、首页文案模块、处理中状态和居中错误弹窗的明确验收标准。
11. 增加全后台路由、权限、表格、表单、主题、上传、国际化和异常页的回归矩阵。
12. 增加 48 小时观察期、两级回退和最终清理条件，确保只保留一套系统时仍有可恢复版本。

评审结论：方案可以进入实施，前提是先完成第 7 节的阶段 0。当前两个目录都有大量未提交内容，直接合并会造成用户工作丢失风险。

## 3. 当前基线与事实依据

### 3.1 代码关系

| 项目 | `GEOFlow` | `GEOFlow-UI-V3` |
|---|---|---|
| 当前分支 | `codex/backend-stability-login-repair` | `codex/admin-ui-v3` |
| 当前提交 | `3f18e0f8173dbb32fa50beedb17b53cf79078f78` | `1d81460e380d12605a722fa68051a1aa527a6106` |
| 共同基线 | `3f18e0f8173dbb32fa50beedb17b53cf79078f78` | 同左 |
| 已跟踪未提交文件 | 94 | 57 |
| 未跟踪文件 | 10 | 63 |
| 已跟踪改动量 | `+1598 / -714` | `+2675 / -297` |

UI V3 提交历史直接建立在当前 GEOFlow 提交之上。实施时应把 UI V3 分支与后续 AI 工作台改动导入 `GEOFlow`，保留原提交关系。

两边未提交内容都视为用户资产。实施过程中禁止使用 `git reset --hard`、`git clean`、`git stash -u` 或覆盖式复制。

### 3.2 路由差异

当前路由快照如下：

| 指标 | 18080 系统 | UI V3 候选系统 | 差异 |
|---|---:|---:|---:|
| 后台路由 | 254 | 270 | `+16` |
| 后台 GET 页面 | 97 | 103 | `+6` |
| API 路由 | 28 | 28 | 0 |

新增 16 条后台路由由两部分组成：

- AI 工作台 13 条：页面、会话、消息、Run、计划、审批、取消、重试和指标。
- 账户中心 3 条：账户页、资料更新和密码更新。

最终融合不得减少原有 254 条后台路由，不得改变现有 API 路由契约。新增路由继续通过原有后台鉴权、权限和 CSRF 机制。

### 3.3 数据差异

当前旧系统数据明显更完整，确认继续作为唯一数据源：

| 数据项 | 18080 系统 | UI V3 演示系统 |
|---|---:|---:|
| 数据表 | 76 | 75 |
| 管理员 | 1 | 3 |
| AI 模型 | 4 | 1 |
| AI 可见性记录 | 1800 | 0 |
| 文章 | 123 | 11 |
| 任务 | 10 | 1 |
| 任务运行记录 | 88 | 0 |
| 知识库 | 15 | 1 |
| 站点设置 | 42 | 17 |

旧系统的重要站点基准：

- 当前主题：`geoflow-template-21-enterprise-signature`。
- 站点名和标题：`GEOFlow`。
- 首页模块配置：1900 字节，SHA-256 为 `ffadea45d7a464856035d38fb2cd519a90553951556057f44c3ef2e4c5303563`。
- 首页样式配置：196 字节，SHA-256 为 `4431182f01ef0d92a37bbc88ef00a5e4b1c250da2ee07630b680559c9299c175`。
- `storage/app` 当前约 154 MB。
- 前台主题 Blade 目录 27 个，公开主题目录 26 个。

UI V3 演示库中的 `toutiao-news-20260426`、`GEOFlow Support`、演示管理员和演示内容都不进入最终数据。

### 3.4 当前验证基线

当前 UI V3 工作树已经通过以下聚焦验证：

- 86 个 PHP 测试，937 个断言。
- 22 个 JavaScript 测试。
- 当前 28080 管理后台可以加载 UI V3 页面。
- 当前 18080 前台可以加载旧数据和企业签名主题。

这组结果来自存在未提交内容的候选工作树，只作为当前能力基线。正式发布结论必须来自干净集成工作树、旧数据克隆环境和最终 18080 环境。

当前浏览器会话未完成 18080 后台的真实账号登录验收。实施阶段必须使用正常登录流程补齐，不允许通过测试中间件或绕过鉴权完成验收。

## 4. 融合原则

### 4.1 数据原则

- 旧系统数据库、`APP_KEY`、文件存储、站点设置和前台主题保持原位。
- UI V3 演示数据库只用于对照，不做整库合并，不导入演示 Seeder。
- 数据迁移只增加 AI 工作台所需结构，不覆盖现有业务表内容。
- 所有备份先完成恢复演练，再进入正式迁移。
- 数据校验采用表数量、关键行数、时间范围、配置摘要和文件清单共同判断。

### 4.2 业务原则

- 原有控制器、领域服务、权限、验证、队列和发布流程继续作为业务事实来源。
- UI V3 迁移页面结构、样式、公共组件和交互方式。
- AI 工作台通过能力注册表和领域服务执行操作，模型不直接调用任意路由、SQL、Shell 或文件系统。
- 外部写入必须经过计划、审批、幂等、目标复核和结果对账。
- AI 创建的内容、任务和知识先进入草稿或暂停状态，继续使用原业务审核流程。

### 4.3 发布原则

- UI V3 和 AI Runtime 使用两个独立开关，初次发布时 AI Runtime 保持关闭。
- 候选环境通过后再切换 18080。
- 观察期内保留旧 UI 回退能力，数据始终使用同一套旧系统数据。
- 观察期结束后发布收口版本，删除旧壳层、临时开关和 28080 隔离环境。

## 5. 最终系统边界

### 5.1 保留内容

- `GEOFlow` Git 历史和原有业务代码。
- 18080 对应 PostgreSQL 数据与 Redis 运行配置。
- 当前 `APP_KEY` 和已加密的 AI 模型凭据。
- 当前管理员、文章、任务、内容资产、分发渠道、线索和系统设置。
- 当前前台主题、首页模块、主题文件和上传文件。
- UI V3 公共壳层、页面模板、样式、前端交互和账户中心。
- AI 工作台的 Agent、能力目录、计划编译、审批、执行、恢复、审计和前端交互。
- 原有更新、备份、托管站点、队列和分发能力。

### 5.2 不进入最终系统

- UI V3 演示数据库及其中的演示管理员、演示内容和演示站点设置。
- `UiV3ReviewSeeder` 和 `FrontendDemoSeeder` 在正式环境中的执行入口。
- 28080 专用 Session、端口、数据库卷、Redis、网络和容器名称。
- `GEOFlow-UI-V3` 独立源码目录。
- 旧 Admin UI V2 静态原型和 UI V3 Pilot 静态包作为运行入口。
- 与 UI V3、AI 工作台或其安全链路没有可追溯关系的后台改动。

测试代码可以保留专用 Seeder，但必须只在测试环境显式调用，正式启动和部署流程不得引用。

### 5.3 本次不扩展的范围

- 不修改公开 API 契约。
- 不更换数据库、Redis、Laravel、Laravel AI 或前端框架。
- 不引入新的第三方基础设施和新的生产凭据。
- 不开放 AI 永久删除、系统更新、密钥修改、管理员修改和安全策略修改。
- 不通过浏览器自动化执行第三方平台发布。
- 不改写当前前台模板内容和业务测试数据。

## 6. 最终技术架构

```text
浏览器
  |
  v
geoflow-web :18080
  |-- /build/assets/*  指纹资源，长期缓存
  |-- /reverb/*        同源实时事件
  |-- 主题与上传资源    短缓存
  `-- 动态请求          no-store
          |
          v
      geoflow-app
          |
          |-- PostgreSQL，沿用旧数据目录
          |-- Redis，缓存、锁、队列、Session
          |-- geoflow-queue
          |     |-- ai-workspace-interactive
          |     |-- ai-workspace
          |     |-- geoflow
          |     |-- distribution
          |     |-- theme-replication
          |     `-- default
          |-- geoflow-knowledge-queue
          |-- geoflow-system-update-queue
          |-- geoflow-scheduler
          `-- geoflow-reverb :18081
```

### 6.1 本地与生产拓扑

本地最终入口也使用 Nginx 网关。应用容器只提供内部服务，18080 由 `geoflow-web` 暴露。这样可以统一静态资源缓存、动态页面缓存和 Reverb 同源行为，并保留 UI V3 已验证的首屏防闪烁策略。

生产继续使用现有 `web + php-fpm` 结构，并同步以下缓存规则：

- `/build/assets/`：`Cache-Control: public, max-age=31536000, immutable`。
- 动态 HTML 和后台页面：`Cache-Control: no-store`。
- 非指纹主题、图片和上传资源：短缓存，不设置 `immutable`。
- 隐藏文件与可执行 PHP 路径：直接拒绝。

### 6.2 队列与超时

- `ai-workspace-interactive` 处理意图解析、追问、普通回答和计划生成。
- `ai-workspace` 处理审批后的领域工作流。
- AI 执行任务最长 900 秒，Worker 超时 930 秒，Redis `retry_after` 不低于 960 秒。
- 两条 AI 队列必须有独立等待时间指标和失败记录。
- `geoflow:recover-ai-workspace` 每 5 分钟运行。
- `geoflow:prune-ai-workspace` 每天 02:30 运行。
- 调度任务使用单机锁和防重叠约束。

默认 Compose 继续使用独立 `queue:work` 容器。`config/horizon.php` 同步维护两条 AI 队列的 Supervisor 配置，供启用 Horizon 的部署使用。两种消费模式只能启用一种，避免重复消费者和重复监控结论。

Reverb 作为实时体验增强。连接失败、事件丢失或序号不连续时，页面自动读取 Run 权威快照，不能依赖前端动画推断任务完成。

## 7. 实施阶段

每个阶段都有独立交付物和门禁。任一门禁失败时停止后续阶段，保留当前可运行版本。

### 阶段 0：保护现场与冻结基线

目标：确保任何合并操作都不会覆盖两边现有工作。

执行内容：

1. 在 `GEOFlow` 为 Admin UI V2 预览改动建立独立归档分支和提交。
2. 在 `GEOFlow-UI-V3` 把已提交 UI V3、未提交 AI 工作台、错误弹窗和分发安全改动拆成可追溯提交。
3. 把 UI V3 分支导入 `GEOFlow` 的本地 Git 对象库，保留原始提交哈希和归档引用。
4. 在 `GEOFlow` 创建 `codex/geoflow-ui-v3-unification` 集成分支和独立干净工作树。
5. 将两边状态、提交、未跟踪文件和差异摘要写入迁移清单。
6. 生成旧数据库逻辑备份、文件归档、文件清单和校验值，放入仓库外的 `../GEOFlow-recovery/20260823/`。
7. 从备份恢复一套临时数据库，验证备份可以使用。

阶段门禁：

- 两个原目录无文件丢失。
- 两边 WIP 都有可定位的归档引用。
- 数据库备份可以成功恢复。
- 文件清单包含数量、总字节数和 SHA-256。
- 集成工作树干净，基础测试可以启动。

### 阶段 1：导入 UI V3 公共壳层与全后台页面

目标：在旧数据克隆环境中运行完整 UI V3，不接入 AI 执行。

执行内容：

1. 导入 UI V3 已提交序列和公共组件。
2. 合并 `AdminUiRegistry`、侧栏、顶部栏、最近处理、账户区、页面标题、表格、表单、弹窗和响应式布局。
3. 保留现有 254 条后台路由和原业务控制器。
4. 增加账户中心 3 条路由，并沿用后台鉴权策略。
5. 在旧数据克隆环境验证 97 条原 GET/HEAD 路由和 6 条新增 GET/HEAD 路由，并用页面清单区分可视页面、数据接口、下载和跳转。
6. 验证普通管理员、超级管理员和登录失效三种身份。
7. 将本地 Nginx 网关接入候选环境，检查资源缓存和页面刷新。

阶段门禁：

- 原有后台路由数量和命名保持完整。
- 旧数据克隆中的表格、长文本、分页、空状态和异常状态正常。
- 菜单、最近处理和快捷入口不会展示无权限路由。
- 表单字段、CSRF、验证错误、上传、导出、删除确认和轮询行为保持兼容。
- 主题和前台页面继续使用旧设置。
- 页面刷新无旧样式闪现，浏览器控制台无未处理错误。

### 阶段 2：完成 AI 工作台页面体验

目标：完成用户已经确认的首页、输入、加载、历史和错误体验，运行时继续关闭。

#### 首页布局

- 首页内容轨道固定为 `720px`，输入框、标题区、快捷建议、运行提示、能力说明卡和安全说明使用同一宽度。
- 对话态内容轨道为 `840px`，保证消息阅读和长计划展示空间。
- 页面外层最大宽度为 `1040px`，只用于居中和响应式留白。
- 手机端使用 16px 页面边距，输入区和操作区不产生横向滚动。
- 历史会话只放在左侧“最近处理”，页面内不再出现第二套历史栏。

#### 输入与提交

- 文本框可以输入、粘贴和多行编辑，最大 4000 字。
- 空内容、纯空格和提交中的重复点击不得发送。
- `Enter` 发送，`Shift + Enter` 换行，中文输入法组合状态下不误发送。
- 提交后保留消息内容到本地和会话记录，失败时恢复原输入。
- 新建会话、历史会话切换和页面刷新后状态一致。
- 发送按钮有禁用、可提交、处理中和重试四种状态。

#### Agent 处理中样式

- 发送后 1.5 秒内显示“正在理解任务”。
- 随后按真实状态显示“正在补充信息”“正在生成计划”“等待确认”“正在执行”“正在整理结果”。
- 使用轻量脉冲点、骨架行和步骤状态，不使用模拟百分比。
- 处理超过正常时间时显示解释和取消按钮。
- Reverb 中断时显示“正在同步最新状态”，并自动回退到快照轮询。

#### 居中错误弹窗

错误弹窗固定在页面正中，使用遮罩和单一焦点。页面顶部不再重复显示同一条错误 Toast。

错误类型与按钮：

| 场景 | 标题 | 主按钮 | 次按钮 |
|---|---|---|---|
| 运行时关闭 | 暂时无法发送这条消息 | 有权限时“去配置 AI 模型”，无权限时“我知道了” | “继续编辑” |
| 模型未配置或未通过检测 | AI 模型还未准备好 | “去 AI 配置器” | “保留内容并关闭” |
| 网络或服务暂时不可用 | 连接没有完成 | “重新发送” | “继续编辑” |
| 登录失效 | 登录状态已失效 | “重新登录” | “复制输入内容” |
| 权限不足 | 当前账号无法执行此操作 | “返回 AI 工作台” | “查看可用能力” |
| 结果未知 | 操作结果需要人工确认 | “查看分发记录”或对应业务记录 | “关闭” |

弹窗必须满足以下要求：

- `role="alertdialog"`、标题和描述关联完整。
- 打开后焦点进入弹窗，Tab 焦点锁定，Esc 和关闭按钮行为一致。
- 关闭后焦点回到原触发位置。
- 用户输入不会因为弹窗打开、关闭或跳转而丢失。
- 后端错误只展示经过脱敏的用户文案，日志保留可追踪错误码。

阶段门禁：

- 输入、发送、快捷键、失败恢复和重复提交测试通过。
- 六类错误弹窗的文案、按钮、权限和焦点行为通过。
- 1440、1280、1024、768、390、320 六种宽度通过截图对比。
- 中文、英文、日文、西班牙文、俄文和葡萄牙文无缺失翻译键。

### 阶段 3：接入 AI Agent 与数据库结构

目标：把真实 Agent 链路合入最终代码，保持 `GEOFLOW_AI_WORKSPACE_RUNTIME_ENABLED=false`。

执行内容：

1. 导入 `app/Ai`、`app/Services/AiWorkspace`、AI 请求、控制器、模型、事件、Job、命令和测试。
2. 增加 13 条 AI 工作台后台路由。
3. 应用 AI 工作台迁移。
4. 接入两条队列、队列监控、可选 Horizon 配置、恢复任务、清理任务和 Reverb 频道。
5. 合并 AI 能力所必需的分发租约、幂等和结果未知处理。
6. 对每一个伴随改动建立“用户目标 -> 能力 -> 文件 -> 测试”追踪，移除无关系的代码漂移。
7. 保持所有系统写操作关闭，验证只读历史、Run 快照和错误弹窗。

数据库变化：

| 类型 | 内容 |
|---|---|
| 新表 | `agent_conversations` |
| 新表 | `agent_conversation_messages` |
| 新表 | `ai_workspace_runs` |
| 新表 | `ai_workspace_steps` |
| 新表 | `ai_workspace_approvals` |
| 新表 | `ai_workspace_artifacts` |
| 新表 | `ai_workspace_external_operations` |
| 新字段 | `ai_models.ai_workspace_structured_output_status` |
| 新字段 | `ai_models.ai_workspace_structured_output_verified_at` |

旧库当前没有上述表，迁移后预期由 76 张表增加到 83 张。验收以实际迁移清单和表名为准。

迁移回退规则：

- 发布回退优先关闭 AI Runtime 和回退代码。
- 不默认执行迁移 `down()`。
- Laravel AI 会话表可能被其他 Agent 共用，回退时保留。
- 已经创建的草稿、分发记录、审批摘要和外部结果保留。
- 需要调整结构时新增前向修复迁移，不修改已执行迁移。

阶段门禁：

- 迁移在旧数据克隆上成功执行，关键业务行数和配置摘要不变。
- `APP_KEY` 未变化，旧 AI 模型密钥可以正常解密。
- 关闭运行时后，新消息和写操作返回受控 503，历史、快照和取消入口保持可用。
- 队列超时、`retry_after`、失败回调、租约和调度配置通过自动化测试。
- 270 条后台路由完成权限归属和能力治理摘要校验。

### 阶段 4：模型检测与端到端自查

目标：在旧数据克隆环境中验证从输入到业务结果的完整链路。

执行内容：

1. 使用旧系统 4 个 AI 模型配置，不复制 UI V3 演示密钥。
2. 由超级管理员逐个执行结构化输出检测。
3. 只有状态为 `ready` 的启用模型可以进入工作台候选列表。
4. 先开放普通问答和只读分析，再开放草稿能力，最后验证需要审批的执行能力。
5. 外部分发和远程站点同步使用预览、沙箱或专用测试渠道，不触达真实客户目标。
6. 对异常、重试、取消、Worker 重启、Reverb 断线和过期审批执行故障演练。

端到端场景：

1. 输入普通问题，Agent 正常回答，并显示“本次未执行系统操作”。
2. 输入缺少信息的任务，Agent 提出少量明确追问。
3. 创建任务草稿，确认数据库只新增暂停状态草稿。
4. 创建文章草稿，继续触发现有风险扫描和审核流程。
5. 创建知识草稿，权限、数据分类和内容长度校验生效。
6. 生成分发预览，不发送远程请求。
7. 审批测试分发，重复点击不会重复创建业务记录。
8. 取消排队和执行中 Run，后续步骤停止。
9. 模拟 Worker 中断，恢复任务可以接管安全步骤。
10. 模拟外部请求超时，状态进入 `outcome_unknown`，系统停止自动重试并提供对账入口。
11. 切换历史会话，消息、计划、审批、步骤和结果可以恢复。
12. 清理任务只清除到期完整载荷，不影响其他 Laravel AI 会话。

阶段门禁：

- 固定意图集 Top-1 准确率不低于 90%。
- 必填信息缺失识别召回率不低于 95%。
- 受限与未知操作拦截率 100%。
- 未授权写入、内部重复业务记录和未知结果自动重试均为 0。
- 首个真实状态反馈 P95 不超过 1.5 秒。
- 所有能力调用可以追溯操作者、计划、版本、摘要、审批和结果。

### 阶段 5：18080 正式切换

目标：在短维护窗口内让最终系统接管 18080。

切换前：

1. 冻结集成提交和资源构建产物。
2. 运行全量 PHP、JavaScript、构建、浏览器和数据回归。
3. 关闭 AI Runtime，暂停外部写能力。
4. 停止 Scheduler，等待队列完成当前任务，并检查无活动租约和待确认外部操作。
5. 进入维护模式。
6. 重新生成 PostgreSQL 逻辑备份和文件清单，完成快速恢复检查。

切换中：

1. 在 `GEOFlow` 发布集成代码和 Vite 资源。
2. 保留旧 `.env` 与 `APP_KEY`，只合并已审核的新环境变量。
3. 执行数据库迁移。
4. 清理并重建配置、路由、视图和事件缓存。
5. 启动 PostgreSQL、Redis、App、Web、Reverb 和各队列。
6. 验证健康检查后启动 Scheduler。
7. 退出维护模式。

切换后：

1. 验证登录、退出、Session、账户中心和权限。
2. 验证前台首页、主题、首页模块、文章详情、表单与上传资源。
3. 验证后台高频页面、表单、队列、导出、分发预览和系统更新页。
4. 验证 AI Runtime 关闭时的居中引导弹窗。
5. 由超级管理员重新完成模型检测，再分批开启 AI Runtime。

服务恢复顺序固定为：

```text
PostgreSQL / Redis
  -> App / Web
  -> Reverb
  -> AI interactive queue
  -> AI workflow queue
  -> 原业务 queues
  -> Scheduler
```

阶段门禁：

- 18080 成为唯一对外管理入口。
- 旧数据关键行数、站点设置摘要、文件清单和当前主题一致。
- Vite 构建清单存在，页面不引用 28080 资源。
- 所有队列可消费，Scheduler 不重复运行。
- 错误率、队列等待和 `outcome_unknown` 无异常增长。

### 阶段 6：观察、收口与删除独立系统

观察期为 48 小时，并至少覆盖一次每日 AI 清理任务、多个五分钟恢复周期和完整业务回归。

观察期内：

- UI V3 默认开启，旧壳层保留为紧急回退。
- AI Runtime 可以独立关闭。
- 28080 停止对外使用，保留为只读候选环境。
- 每 4 小时检查错误日志、队列失败、恢复次数、模型调用、数据库增长和静态资源 404。

满足以下条件后发布收口版本：

- 没有阻断级 UI、业务、数据或权限回归。
- 没有未对账的 `outcome_unknown`。
- 旧数据和文件校验持续一致。
- 全量自动化和人工关键路径再次通过。
- 归档提交、数据库备份和文件备份可读取。

收口操作：

1. 删除旧 UI 壳层及临时 `GEOFLOW_ADMIN_UI_V3_ENABLED` 分支逻辑。
2. 删除 UI V2 和 UI V3 Pilot 的运行入口。
3. 删除 `.env.ui-v3`、`docker-compose.ui-v3.yml` 和 28080 专用开发脚本。
4. 停止并删除名称以 `geoflow-ui-v3-` 开头的容器、网络和命名卷。
5. 确认 UI V3 分支已导入 `GEOFlow` 并存在归档引用后，删除 `GEOFlow-UI-V3` 独立目录。
6. 更新 README、部署文档、运行手册和系统更新说明，只保留 18080 单系统操作方式。

删除范围必须精确匹配 `geoflow-ui-v3-*` 和 `GEOFlow-UI-V3`。不得删除 `GEOFlow/docker-data`、`GEOFlow/storage`、`geoflow-postgres` 或其旧数据目录。

## 8. 环境变量合并清单

### 8.1 必须保留旧值

- `APP_KEY`、`APP_URL`、`SITE_URL`、`ADMIN_BASE_PATH`。
- 数据库、Redis、Session 和缓存连接。
- Mail、对象存储、分发渠道、托管站点和出站网络配置。
- Reverb 应用 ID、Key、Secret、Host 和允许来源。
- 当前 AI Provider 和模型密钥。
- 系统更新、备份和遥测开关。

### 8.2 新增 AI 工作台配置

- `GEOFLOW_AI_WORKSPACE_RUNTIME_ENABLED=false`
- `GEOFLOW_AI_WORKSPACE_RETENTION_DAYS=90`
- `GEOFLOW_AI_WORKSPACE_GLOBAL_CONCURRENCY=10`
- `GEOFLOW_AI_WORKSPACE_CONCURRENCY_CACHE_STORE=redis`
- `GEOFLOW_AI_WORKSPACE_ADMIN_DAILY_MODEL_CALLS=200`
- `GEOFLOW_AI_WORKSPACE_HISTORY_CHAR_BUDGET=24000`
- `GEOFLOW_AI_WORKSPACE_MAX_ACTIVE_RUNS_PER_ADMIN=3`
- `GEOFLOW_AI_WORKSPACE_MAX_PLAN_STEPS=100`
- `GEOFLOW_AI_WORKSPACE_STEP_LEASE_MINUTES=20`
- `GEOFLOW_AI_WORKSPACE_RESOLUTION_LEASE_MINUTES=3`
- `GEOFLOW_AI_WORKSPACE_REQUIRE_VERIFIED_MODEL=true`
- `GEOFLOW_AI_WORKSPACE_APPROVAL_TTL_MINUTES=30`
- `GEOFLOW_AI_WORKSPACE_RESOLUTION_THRESHOLD=0.85`
- `GEOFLOW_AI_WORKSPACE_READ_BUDGET_ROWS=500`
- `GEOFLOW_AI_WORKSPACE_QUEUE=ai-workspace`
- `GEOFLOW_AI_WORKSPACE_INTERACTIVE_QUEUE=ai-workspace-interactive`

### 8.3 观察期临时配置

- `GEOFLOW_ADMIN_UI_V3_ENABLED=true`

该开关只服务于 48 小时观察和紧急 UI 回退。阶段 6 收口版本删除开关与旧壳层，UI V3 成为唯一实现。

### 8.4 必须关闭的演示配置

- `GEOFLOW_SEED_FRONTEND_DEMO=false`
- `GEOFLOW_SEED_FRONTEND_DEMO_OVERWRITE=false`

任何 `.env`、密钥、Token 和数据库密码都不进入 Git、迁移报告或截图。

## 9. 数据与文件验收

### 9.1 数据库

迁移前后至少核对：

- 76 张原表全部存在，迁移新增 7 张 AI 表。
- 管理员 1、AI 模型 4、AI 可见性记录 1800、文章 123、任务 10、任务运行 88、知识库 15、站点设置 42 的迁移前基线不减少。
- 每张关键表记录最早和最晚更新时间，识别意外覆盖或时间戳重写。
- 当前主题、首页模块、首页样式和站点名称保持原值。
- PostgreSQL 的 pgvector 扩展和现有索引可用。
- 新表外键、唯一键、状态索引和级联行为符合迁移定义。

迁移期间业务数据仍可能增长，正式比较以维护窗口内生成的最终快照为准。新增业务记录可以增加，原记录不得静默减少。

### 9.2 文件

- `storage/app` 使用归档复制，保留权限、目录、符号链接语义和修改时间。
- 生成迁移前后文件数量、总字节数和 SHA-256 清单。
- 验证 `public/storage` 链接。
- 验证上传、主题资源、托管站点资源、主题生成预览和系统更新备份目录。
- 验证 `public/build/manifest.json` 与实际指纹资源一致。
- 禁止把 UI V3 的 2.3 MB 演示存储覆盖到旧系统 154 MB 存储。

## 10. 测试与验收矩阵

### 10.1 自动化

正式发布前运行：

```bash
vendor/bin/pint --dirty --format agent
php artisan test --compact
node --test tests/JavaScript/*.test.js
npm run build
composer audit
php artisan route:list --path=admin -v
php artisan schedule:list
docker compose config
```

AI 聚焦测试至少覆盖：

```bash
php artisan test --compact tests/Unit/AiWorkspace
php artisan test --compact tests/Feature/AdminAiWorkspaceTest.php
php artisan test --compact tests/Feature/AiWorkspaceWorkflowTest.php
node --test tests/JavaScript/ai-workspace.test.js
```

所有发布结论必须来自干净集成工作树。若全量测试存在与本次无关的既有失败，报告需提供失败用例、基线对照和影响判断，阻断级失败不能带入切换。

### 10.2 页面与业务

| 维度 | 验收范围 |
|---|---|
| 视口 | 1440、1280、1024、768、390、320 |
| 身份 | 超级管理员、普通管理员、登录失效 |
| 页面状态 | 正常、加载、空数据、错误、无权限、部分数据缺失 |
| 列表 | 搜索、筛选、排序、分页、批量操作、宽表格 |
| 表单 | 创建、编辑、验证失败、未保存提醒、上传、删除确认 |
| 实时 | 轮询、Reverb、断线恢复、旧事件丢弃、序号缺口恢复 |
| 国际化 | 六种现有后台语言、长文案、数字和时间格式 |
| 可访问性 | 键盘、焦点、弹窗锁定、图标标签、降低动画偏好 |
| 异常页 | 403、404、419、422、429、500、503 |
| 前台 | 首页、文章、主题、首页模块、表单、上传资源 |
| 运维 | 更新、备份、队列、Scheduler、日志、健康检查 |

### 10.3 浏览器关键链路

1. 正常登录 18080 后台。
2. 访问数据中心、任务、内容、资产、分发、AI 配置和网站设置。
3. 创建并编辑一条可删除测试记录，验证表单和权限。
4. 进入 AI 工作台，输入文字，发送普通问答。
5. 创建草稿、审批测试操作、查看步骤和结果。
6. 刷新页面并切换历史会话，验证恢复。
7. 关闭 Runtime，确认居中错误弹窗与引导按钮。
8. 断开 Reverb，确认轮询继续更新真实状态。
9. 查看前台页面，确认主题和旧数据未变化。

## 11. 回退方案

### 11.1 UI 回退

观察期内将 `GEOFLOW_ADMIN_UI_V3_ENABLED=false`，刷新配置缓存并重启 App/Web。页面回到旧壳层，数据库和业务数据不变。

### 11.2 AI 回退

将 `GEOFLOW_AI_WORKSPACE_RUNTIME_ENABLED=false`，刷新配置缓存并重启相关 Worker。系统停止接收新消息、审批、计划修改和重试，继续允许查看历史、Run 快照和取消执行中任务。

### 11.3 发布回退

回到上一个已验证镜像或提交，恢复旧 Nginx 路由。保留新增 AI 表和列，不执行默认 `down()`。若数据库迁移本身失败，在维护窗口内使用迁移前逻辑备份恢复，并同步恢复对应文件快照。

### 11.4 外部操作处理

- 已确认成功的外部操作保留记录，不做自动反向操作。
- 已发出且无法确认的操作进入 `outcome_unknown`，人工对账后处理。
- 未发出的步骤安全取消。
- 回退过程禁止对 `outcome_unknown` 自动重试。

## 12. 风险与处理

| 风险 | 触发条件 | 处理 |
|---|---|---|
| 两边 WIP 被覆盖 | 直接复制或切分支 | 阶段 0 先归档提交，使用独立干净工作树 |
| 演示数据覆盖旧数据 | 运行 UI V3 Seeder 或复制 `.env` | 正式禁用演示 Seeder，只用旧 `.env` 合并新键 |
| 加密密钥失效 | `APP_KEY` 改变 | 保留旧值并在克隆库验证模型密钥解密 |
| 大数据页面布局退化 | 只在小演示库验收 | 所有页面必须在旧数据克隆验证 |
| Worker 重复执行 | 超时与 `retry_after` 配置错误 | 保持 900/930/960 秒边界，校验幂等和租约 |
| 外部写结果不确定 | 网络超时发生在远端成功之后 | 写操作账本、稳定幂等键、`outcome_unknown` 和人工对账 |
| 页面刷新闪旧样式 | 动态页或资源缓存错误 | Nginx 分离指纹资源、普通资源和动态 HTML 缓存 |
| 权限入口泄露 | 菜单、最近处理与路由判断不一致 | 共用路由权限注册表，按身份跑全页回归 |
| 回退删除会话数据 | 执行共享会话表 `down()` | 以开关和代码回退为主，保留共享表 |
| 清理误删旧数据 | 容器或目录目标过宽 | 只匹配 `geoflow-ui-v3-*` 和 `GEOFlow-UI-V3`，清理前复核清单 |

最脆弱的假设是 UI V3 在小型演示数据上成立后，也能直接承载旧系统更大的列表、长文本、复杂设置和 154 MB 文件。旧数据克隆验收是正式切换的硬门禁，任何关键页面失败都会暂停切换。

## 13. 完成定义

同时满足以下条件，融合才算完成：

- `GEOFlow` 是唯一代码库和源码目录。
- 18080 是唯一管理后台入口，28080 已下线。
- 旧数据库、旧 `APP_KEY`、旧站点设置、旧主题和旧文件继续使用。
- 全后台页面统一采用 UI V3，旧壳层和原型运行入口已删除。
- AI 输入、提交、Agent 处理、加载状态、审批、执行、错误弹窗、历史和恢复链路可用。
- 新增 AI 数据结构、队列、调度、Reverb、清理和恢复任务正常。
- 全量自动化、浏览器关键路径、数据校验和 48 小时观察通过。
- UI V3 独立容器、网络、数据库卷和源码目录已删除。
- 最终优化报告包含变更范围、测试结果、数据校验、性能与错误指标、遗留风险和回退记录。

## 14. 用户确认后的执行顺序

用户确认本文件后，按以下顺序实施：

```text
阶段 0 保护现场与备份
  -> 阶段 1 UI V3 全后台融合
  -> 阶段 2 AI 工作台体验完成
  -> 阶段 3 Agent、迁移与基础设施接入
  -> 阶段 4 旧数据克隆端到端自查
  -> 阶段 5 切换 18080
  -> 阶段 6 观察、收口和删除独立系统
  -> 生成最终优化报告
```

在用户确认之前，不执行代码合并、数据库迁移、容器切换或删除操作。

# GEOFlow 更新日志

该文档记录公开仓库可见功能的持续更新。后续每次推送到 GitHub 时，同步更新本文件和英文版 `CHANGELOG_en.md`。

## 2026-08-31

### AI 质检三层召回与原子事实治理

- 新增原子质检、切片质检、知识库质检三种召回方式，任务设置与单篇文章编辑页共用可用性判断、默认优先级、继承与覆盖规则；原子质检在准备度满足时优先选中。
- 建立召回准备度、服务代次、来源依赖账本和不可变执行快照，质检记录保存请求方式、实际方式、策略版本、来源哈希与审计信息；知识、模型、提示词、正文和 rollout 变化会选择性使旧结果失效并触发对账。
- 完成受治理原子事实库的生成、审核、版本发布、稳定键、证据重连、类型化比较和活动版本服务能力；原子路径覆盖确定性事实，未覆盖或歧义主张继续进入切片检索。
- 加固高风险知识审核、宽范围正文证据预算、提示词注入隔离、跨令牌 API 幂等、并发策略版本校验、分发门禁快照和知识库引用删除保护。
- 扩展管理端知识库导航、原子事实工作台、AI 模型诊断和六种语言文案，并增加历史数据回填命令、Docker 队列配置、Laravel 与 JavaScript 回归测试及评测报告。

## 2026-08-30

### 许可与贡献治理

- 从本次变更起，GEOFlow 的新版本和仓库修订采用 GNU Affero General Public License v3.0 only；此前按 Apache-2.0 发布的版本继续适用其原有许可证。
- 闭源修改、白标、OEM、专有产品集成及其他无法履行 AGPL-3.0 义务的场景，可以向版权所有者申请单独的商业许可。
- 新增 Contributor License Agreement、贡献指南和 Pull Request 声明流程。贡献者保留版权，同时授予项目维护者在 AGPL 开源版本及商业或专有许可版本中使用贡献所需的转授权权利。

### 产品与体验更新

- 新增文章 AI 质检自动优化闭环：管理员可选择达标、80 分优秀或 90 分优秀目标，系统按轮次生成候选修改、重新质检、展示分数与变更记录，并支持应用、取消、回滚和异常任务对账。
- 提升 AI 质检稳定性与可解释性：优化长文检查、证据定位、结果校验、评分与失效策略，补充专用队列、健康检查、额度控制和版本化提示词，任务页可配置自动优化策略。
- 统一后台高风险操作交互：文章、任务、模型、素材库和知识库等页面共用操作确认对话框，明确操作对象、影响范围、输入校验、处理中状态与失败恢复提示。
- 完善 Admin UI V3 页面体验：文章质检结果支持折叠，顶栏与页面身份更清晰；每个后台页面底部统一展示版本、更新日志、许可证、版权、作者 X 主页、GitHub 仓库和帮助入口，并兼容短页面贴底与移动端换行。
- 扩展 API v1 与 GEOFlow CLI 的文章 AI 优化能力，并为 Docker 部署补充质检与优化队列的超时、重试、健康检查和 Worker 配置。

## 2026-08-29

### 主干更新

- 加固文章回收站与数据完整性：
  - 永久删除文章时保留访问日志，并将 `view_logs.article_id` 安全置空；兼容 PostgreSQL 在线约束替换、SQLite 和其他受支持数据库。
  - 批量永久删除加入 500 篇上限、敏感操作限流、事务和行锁；失败提示隐藏数据库细节，整批操作失败时完整回滚。
  - 作者列表和素材 API 统一统计回收站文章，存在关联文章时继续保护作者记录。
- 优化系统更新中心：
  - 检测到新版本时展示版本号、发布类型、日期、更新摘要和官方 GitHub Release 入口。
  - 更新链接固定指向 GEOFlow 官方仓库，并根据经过校验的标签生成，阻止远端元数据把管理员引导到非官方地址。
  - 人工知识同步步骤补充用途、当前状态和可复制命令；超级管理员可从顶栏更新图标直接进入更新中心。
- 统一 Admin UI V3 页面表达：
  - 顶栏补充页面短标题与语义图标，数据中心和运营工作台减少重复标题与次要操作。
  - 数据中心指标卡统一高度、数字对齐和信息密度；AI 帮助轮播控件缩短无效间距。
  - 欢迎页更新为 GEOFlow 3.0 定位、核心能力、适用场景和四步上手说明。

## 2026-08-28

### v3.0.0

- 升级 AI 工作台系统知识与图文问答：
  - 新增超过 1 万中文字符、覆盖 15 个主题章节的后台功能操作指南，包含功能逻辑、设计原理、流程、亮点、故障处理和可信站内入口。
  - 系统知识库拥有稳定绑定、官方版本、健康状态、受控二次编辑、修订恢复和幂等同步；应用与数据库共同阻止删除，索引故障时继续使用正文回退。
  - 帮助检索只读取系统知识，支持复杂问题的多章节证据、短追问上下文、24,000 字符总预算、结构化来源与权限过滤后的功能入口。
  - 新增 24 张脱敏 WebP 后台截图、私有存储、哈希校验、不可变替换版本、图库管理、相关性选择和历史固定回放；中文回答单次展示 0 至 3 张强相关图片。
  - 新增 72 条固定问答评测、全后台路由分类、知识和图片清单门禁，以及未过期历史引用保护的媒体清理策略。
- 完成独立更新工具 C 批切换：
  - 独立更新工具成为网站更新、完整备份和回滚的唯一执行边界；旧计划、文件覆盖、单文件恢复、重试和人工失败标记入口已移除。
  - 旧运行与备份表继续保留，更新中心按最近 90 天和归档视图提供只读审计查询。
  - 网站发起更新、备份或回滚时需要管理员密码与验证器生成的 6 位授权码；更新工具在宿主机以独占锁持久化已消费时间步，阻止授权码重放。
  - 旧更新队列容器与 Horizon supervisor 已退出运行配置；兼容 tombstone 任务只把遗留队列记录标记为已停用，不执行文件、数据库或容器变更。
  - 出站响应总上限迁移到独立配置，旧执行器配置已清理；发布清单新增 amd64 与 arm64 真实宿主演练门禁。
- 完成独立更新工具 B 批接入：
  - 系统更新中心新增安全更新、完整备份、环境验收和恢复点一键回滚入口，敏感操作要求超级管理员确认当前密码。
  - 页面展示持久化操作编号、当前阶段、阶段结果和最近恢复点；操作执行期间自动刷新状态并阻止并发提交。
  - 网站通过固定类型的本地 Unix socket API 发起操作，接口不接受任意命令或文件路径。
  - 更新工具在迁移和版本切换前备份 PostgreSQL、完整站点存储、持久化 Redis 数据、环境配置、版本文档和受管部署状态；受保护阶段失败时自动恢复并验收。
  - 恢复点在写入后记录摘要、大小、权限和属主，恢复前完成全量校验，默认保留最近 5 个。
  - 操作中断后由持久化 recovery_required 状态和跨进程锁协调恢复；新旧执行器双向检查运行状态，避免后台入口和队列任务交叉执行。
  - 管理员密码不进入审计载荷或失败回填，代理异常仅写入服务端日志；应用侧严格校验 Unix socket 响应结构和长度。
- 新增独立更新工具 A 批接入：
  - 系统更新中心新增 GEOFlow Updater 连接状态、环境诊断摘要、已签名安装包准备与私有下载入口。
  - 安装包经过内置 2/3 离线可信根、targets 角色签名、平台、大小和 SHA-256 校验，下载请求统一经过出站安全网关。
  - 安装包私有暂存状态包含签名有效期并在下载时重新校验文件类型、路径、大小和摘要，符号链接、过期状态和被修改的文件会被拒绝。
  - 网站仅通过带实例令牌的本地 Unix socket 读取更新工具状态，不挂载 Docker socket。
  - 首次托管接管命令会加载站点与签名发布两份环境文件，在挂载同一数据库目录前显式停止标准生产项目，并提示排空队列和预留维护窗口。
  - A 批建立独立工具的安装、信任和连接基础，B 批在此基础上接入事务操作。

- 新增文章 AI 质检工作流：
  - 任务新建与编辑支持默认关闭的 AI 质检开关，可选择质检方案、执行模型、自动通过分和人工放行最低分。
  - 每篇启用质检的文章先结合知识库、版本化广告规则和发布语境完成异步检查，由后端固定规则计算总分与四项分数。
  - 通过结果继续原审核与发布周期；待复核、阻断、异常和过期结果保留为草稿，并统一阻止本地发布、站点分配、人工发布和渠道分发。
  - 文章列表与详情展示状态、分数、结论、问题颜色、原文定位、证据、法规依据、修改建议和历史记录，并提供重检与审计化人工放行。
  - 文章、任务策略、提示词、模型、知识切片和法规版本变化会使旧结果过期，调度任务负责补偿重检。
  - 长文章按分段续派独立队列任务，每个模型的结构化请求与 JSON 回退共享单次时间预算；补偿扫描隔离单篇配置异常并使用游标续派，避免一篇文章阻塞后续质检。
  - 质检执行加入 180 秒端到端截止时间、专用在线与回填队列、超时抽样降级、类型化供应商故障和 Worker 健康检查；失败记录保持“未评分”语义，并给出可执行的重试建议。
  - 新增可灰度启用的 `fast_v2` 紧凑执行、稳定证据键、评分 v2 影子评测、离线与真实模型黄金集命令，以及带事故冻结和恢复报告的分级发布门禁。
- 统一 Admin UI V3：
  - 后台主要页面统一使用新的侧栏、顶栏、导航、表单、对话框和响应式布局，补充最近访问、侧栏宽度调节、键盘操作、移动端与无障碍状态。
  - 图标、字体与页面资源改为本地加载，首屏状态和页面切换经过稳定性检查，减少闪烁、布局跳动与外部资源依赖。
- 调整 AI 工作台边界：
  - 3.0 的 AI 工作台定位为后台帮助助手，使用本地帮助目录、当前管理员权限和一次对话模型调用回答系统使用问题，通过 SSE 返回真实连接状态与流式正文。
  - 功能入口由服务端按命名路由和权限生成，模型输出中的链接不会直接进入可点击区域；模型检测、流式就绪、普通文本降级、超时和故障转移均有明确状态。
  - 旧版 Run、Plan、Approval、Capability 与 Trace 工作流停止接收新请求，已有会话、运行记录和审计数据继续保留。
- 新增托管渠道站点闭环：
  - 支持在托管根域下分配子域名、管理站点生命周期、分配文章、控制发布配额与间隔，并提供暂停、维护、归档、索引和失败冷却状态。
  - 新增技术预检、缓存失效、状态对账和恢复命令，覆盖主站、托管根域、保留子域、可信代理、Nginx 三入口、泛 DNS 与通配符 TLS 边界。
  - 托管站点默认关闭，完成网络、证书和反向代理配置后再显式启用。
- 连接人工发布、Chrome 运营助手与 PWA：
  - Chrome 扩展通过短时设备码完成管理员批准，只获得浏览器运营读取与执行权限；支持领取、心跳、恢复、账号校验、执行回执与 Token 撤销。
  - 首个适配器可在知乎问题页填充纯文字回答草稿，用户检查后完成最终发布；通用模式继续支持打开目标页、复制正文和人工回填结果。
  - 后台可安装为 PWA，提供站内图标、清单、Service Worker、更新提示与独立窗口体验。
- 完善任务、模型和人工发布体验：
  - 任务保存、启动和队列执行前统一检查标题库容量、循环策略与受保护任务冲突，容量不足时提供可执行的补充建议和管理入口。
  - AI 模型表单拆分为可复用页面，连接检测记录真实流式能力与普通文本降级状态；正文生成默认输出上限提升到 16K token。
  - 人工发布补充浏览器载荷、领取租约、执行凭证、结果待核验状态和更严格的账号与目标地址检查。
  - 标题库支持最高 10 万条的分批 AI 生成、进度恢复、取消、失败重试和稳定去重；任务删除进入保留 90 天审计信息的回收站，文章列表支持批量导出 Markdown ZIP。
  - 保留 v2.3 已发布的人工发布身份快照、完整状态历史、锁内执行人复核、90 天精确重复检测和可搜索分页文章选择器，现有数据库与全新安装使用一致结构。
- 扩展 API v1 与 GEOFlow CLI：
  - `bin/geoflow` 0.2.0 覆盖目录、任务、执行记录、素材和文章，支持安全配置、登录、JSON 文件或 stdin、删除确认和结构化错误。
  - API v1 增加浏览器设备授权、会话和人工发布协议，并继续保留权限、幂等、版本协商和错误契约。
- 整理安装、升级与部署边界：
  - 全新安装默认只创建必要数据，不自动导入演示文章；已有站点继续保留主题、设置、分类、文章和业务数据。
  - 升级前需要备份数据库、`.env`、上传目录与 `storage`，停机排空旧进程后执行迁移、前端构建和进程重启；Docker 启动命令加入 `--remove-orphans` 清理已退出当前编排的旧服务。
  - 从早期 2.x 版本升级时继续完成受管图片 readiness 和 `geoflow:security-audit`，使用 Chrome 运营助手的部署还需同步更新扩展。
  - 前台 `/archive` 与按月归档 URL 继续提供内容列表和主题模板，保留 v2.3 已公开链接的访问与索引语义。
  - 匿名统计地址留空时不发送浏览器活跃请求；部署方需要同时显式启用开关并配置 HTTPS 采集地址。
- 组件兼容与发布门禁：
  - GEOFlow 为 `3.0.0`，内置 CLI 为 `0.2.0`，Chrome 运营助手为 `0.1.0`。
  - 独立更新工具必须使用发布授权明确绑定 GEOFlow `3.0.0` 最终提交及 app/web 镜像摘要的签名版本；正式发布前完成 amd64 与 arm64 真实宿主演练，并在 GitHub Release 中写明精确兼容版本。

## 2026-08-09

### v2.3.0

- 新增企业签名版前台主题 `geoflow-template-21-enterprise-signature`：
  - 提供首页、分类、文章、关于、归档与分页模板，配套独立 CSS、JavaScript、设计令牌、模块映射和预览说明。
  - 首页支持核心卖点、能力矩阵、案例、内容与线索表单模块；没有选定有效表单时自动显示演示表单，选定后接入真实提交链路。
  - 已覆盖 1280 px 桌面端和 375 px、320 px 移动端预览；主题已发布，全新部署完成初始化后默认启用。
- 新增版本化官网参考内容包 `frontend-reference-v1`：
  - 收录 50 篇 Markdown 文档与统一 JSON 元数据，按“功能指南”35 篇、“部署运营”15 篇组织。
  - 全新空库执行 `geoflow:install` 时默认导入参考内容并启用 21 号主题；`--without-demo` 保留极简首次安装选项。
  - 已存在安装标记或业务数据的站点会跳过导入，并保留当前主题、站点设置、作者、分类和文章。
- 新增后台 UI V2 独立原型：
  - 收录 8 组、84 个页面状态，覆盖工作台、内容、素材、分发、数据分析、站点、AI 配置和系统管理。
  - 原型使用只读演示数据，与生产接口隔离，并提供构建脚本、页面清单、组件画廊和自动校验。
- 新增人工发布工作台：
  - 支持从已审核文章创建发帖工单，也可为公开目标地址创建评论工单，并记录身份、平台账号、执行人、计划时间、风险提示和重复提醒。
  - 支持待执行、执行中、已完成、失败、跳过和取消状态流转，保留身份与账号快照、完整状态历史、发布地址和结果备注，并提供筛选、统计和 CSV 导出。
  - 文章选择器支持服务端搜索与分页，并始终保留当前工单关联文章；精确内容、目标地址与来源文章的重复检测覆盖完整 90 天窗口。
  - 状态流转会在数据库锁内重新校验当前执行人，避免并发改派后原执行人继续操作。
  - 普通管理员仅能处理分配给自己的工单；工作台不保存外部平台密码、Cookie、Token 或 OAuth 凭证。
- 更新 AI 模型与出站请求运行时：
  - 官方 OpenAI 聊天模型使用 Responses API，第三方 OpenAI 兼容服务使用对应兼容驱动，并补充 Gemini 新模型参数和 Atlas Cloud 预设。
  - 模型连接测试计入额度与用量审计，上游错误在展示前完成密钥和令牌脱敏；DNS 解析减少已有地址场景下的重复查询。
- 补充文章、线索表单、分发、站点设置和管理入口之间的联动，并扩展主题、人工发布、AI 运行时、权限与出站安全测试。

## 2026-07-29

### v2.2.0

- 新增匿名成功登录统计：
  - 后台 Web 与 API 登录成功后分别发送 `admin_login` 事件，并记录渠道、当前版本、随机实例标识和管理员不可逆摘要。
  - 每次成功事件使用独立 UUID 去重，中央采集端可准确汇总登录次数和匿名管理员活跃趋势。
  - 载荷字段采用固定白名单，不包含用户名、邮箱、IP、域名、页面路径、登录失败信息、内容或密钥。
- 保持登录可用性边界：
  - 统计请求在响应完成后延迟执行，公网采集超时或失败不会改变 Web 与 API 登录结果。
  - 登录失败、遥测关闭和采集地址缺失时均不发送中央请求。
- 扩展自动化测试，覆盖匿名载荷、Web/API 渠道、失败登录静默和遥测网络故障场景。

## 2026-07-28

### v2.1.2

- 更新前端依赖安全基线：
  - 升级 Axios、Vite、esbuild、PostCSS、concurrently、shell-quote、Pusher JS 等兼容补丁版本。
  - 移除旧 `engine.io-client` / `ws` 依赖链，npm 生产与开发依赖审计均恢复为 0 条已知漏洞。
- 修正匿名监控数据口径：
  - 服务端安装、更新和每日心跳独立统计部署数、活跃部署与版本分布。
  - 浏览器 `admin_active` Pulse 只统计管理员日活，不再抬高部署指标。
  - Cloudflare D1 按生命周期版本、每日心跳和每日管理员摘要去重，网络重试不会重复扩大原始事件数量。
- 统一 PHP 代码格式基线，Pint 全量检查恢复通过。

## 2026-07-17

### v2.1.1（发布准备）

- 新增轻量匿名部署统计：
  - 首次安装发送 `installed`，检测到版本变化发送 `updated`，调度器每天最多发送一次 `heartbeat`，用于统计已发现部署、活跃部署和版本分布。
  - 保留后台浏览器 `admin_active` Pulse，继续按“随机实例 ID + 管理员不可逆摘要”统计管理员日活。
  - 默认通过 Cloudflare Pages Functions HTTPS 入口写入 D1，部署方可替换采集地址或完整关闭统计。
  - 服务端载荷限定为事件类型、随机实例 ID 和版本；网络失败不会改变安装、升级和调度结果，可通过 `GEOFLOW_TELEMETRY_ENABLED=false` 关闭。
- 加固前台结构化数据输出：
  - 所有主题的 JSON-LD 统一使用 Laravel `Js::encode` 生成，阻断 `</script>` 等可执行上下文注入，同时保留合法 Schema 数据。
- 收紧受管图片和 API 幂等边界：
  - 图片上传改用内容寻址文件名和受管目录校验，新增 `images.managed_path_hash`、`managed_image_paths` 注册表、状态与 fencing 信息，避免外部路径、符号链接和并发删除越界。
  - API 幂等记录新增 durable state、owner lease 与 fingerprint version，旧记录和过期的 `in_progress` 记录进入显式人工处理路径。
  - 物理图片删除保持默认关闭，升级必须停机排空旧进程、确认迁移、完成 `managed_path_hash` 回填和 readiness 检查后再开启。
- 统一出站请求与敏感后台权限：
  - 分发、URL 导入、主题参考抓取、AI、更新检查与归档下载统一经过出站安全网关，覆盖 URL 规范化、DNS 全量校验、IP 固定、重定向控制、响应体上限和错误脱敏，修复 SSRF 绕过路径。
  - 分发、URL 导入、主题和复刻相关敏感管理路由统一要求超级管理员权限。
- 隔离生成主题代码：
  - 关闭可写入 Blade/CSS 的在线主题编辑入口和路由；主题复刻预览使用受信任的确定性页面，不编译生成的 Blade 代码，并以 sandbox CSP 禁止脚本与外部资源。
  - 主题复刻发布改为 package-only 审查包下载，不再把生成文件写入运行中的主题目录。
- 新增只读安全审计：
  - `php artisan geoflow:security-audit` 提供人工可读结果，`--json` 提供稳定 schema；任意 finding 或审计无法完成时返回退出码 `1`。
  - 审计覆盖安全迁移、受管图片注册表、删除门禁、API 幂等状态、旧图片路径输入和私网出站例外，不发起网络请求、DNS 查询或数据修复。
- 更新依赖安全基线：
  - 升级 Laravel、Guzzle、PSR-7 和 Symfony 安全补丁，锁文件依赖审计无已知公告。
  - 项目最低 PHP 版本统一为 8.3，与当前 `laravel/ai` 要求一致；Docker 默认继续使用 PHP 8.4。

## 2026-06-26

### v2.1.0

- 新增企业知识库草稿生成工作流：
  - 后台新增“企业知识库”入口，支持创建项目、上传或粘贴多份来源资料、查看来源解析结果、触发 AI 草稿生成和查看历史修订。
  - 新增项目、来源、修订三类数据表和队列任务，草稿生成可在后台异步执行，避免长资料阻塞管理页面。
  - 来源解析支持文本、Markdown、HTML、CSV、JSON、XML、PDF、Word 和 Excel 等常见资料格式，并统一沉淀为可追踪来源。
  - AI 清洗和结构化整理改为更重视原文覆盖，长资料会按模块拆分处理，并把遗漏的来源事实回填到对应章节，减少上传资料被过度简化的问题。
  - 草稿结果保留模块、目标读者、内容形态、待人工确认项和来源摘要，便于继续人工审核与发布。
- 优化知识库与素材管理：
  - 知识库详情页新增 Markdown 编辑器，支持在后台直接查看和调整知识内容。
  - 知识库详情页支持重新提交语义切片，并能从详情页安全返回。
  - 素材管理页增加企业知识库构建入口，让普通知识库、素材库和企业知识库草稿工作流更容易串联。
- 增强主题、模板和前台输出：
  - 新增站点主题在线编辑能力，可在后台编辑当前主题模板并保留测试覆盖。该在线主题编辑能力已在 v2.1.1 中关闭，当前安全流程使用隔离预览和 package-only 审查包。
  - 站点设置新增首页模块配置和自定义样式能力，目标站点包同步支持更丰富的首页结构。
  - 新增 APIHot 推荐类前台主题，补充首页、分类、归档、文章页和配套静态资源。
  - 统一前台 SEO metadata 输出，主题页面共享 SEO head 逻辑，减少不同主题之间标题、Open Graph 和结构化数据不一致的问题。
- 改善分发管理和目标站点同步：
  - 任务新增分发策略，可按任务配置本地、渠道和本地加渠道的分发方式。
  - 文章列表增强分发状态可视化，方便直接查看远端副本、同步状态和失败信息。
  - 分发管理页支持批量选择启用中的 GEOFlow Agent 渠道并同步目标站点设置。
  - 目标站点包补充统一 SEO metadata 契约，前台和远端静态页面的搜索输出更一致。
- 提升部署与运行稳定性：
  - 新增安装状态标记与默认数据写入保护，避免已有部署在重启、迁移或升级时重复写入演示数据。
  - 默认管理员初始化支持邮箱配置，并补充生产入口脚本、Redis session、Docker 镜像、网络、权限和反向代理相关改进。
  - 后台路径和 Reverb 鉴权路径支持子目录部署，降低 `APP_URL` 带路径前缀时的静态资源和 WebSocket 配置问题。
- 扩展测试覆盖：
  - 新增企业知识库、主题编辑、站点设置、分发策略、目标站点同步、SEO metadata、安装保护和部署配置相关测试。
  - 本次发布前完整测试结果为 `479 passed`、`3179 assertions`。

## 2026-06-02

### v2.0.4

- 修复已部署环境中 `.env` 固定版本号导致后台更新后仍显示旧版本的问题：后台版本默认读取本地 `version.json`，环境变量样例不再写入 `GEOFLOW_APP_VERSION`。
- 重构 Docker 首次安装逻辑：新增 `php artisan geoflow:install` 与系统安装标记，空库才执行默认安装填充；已有数据的旧库只补标记，避免重启或升级后重复写入默认分类、文章、网站设置、广告和提示词。
- 更新后台版本号为 `2.0.4`，同步 `version.json` 和后台默认版本展示。

### v2.0.3

- 新增系统更新中心：
  - 后台右上角通知可进入独立“系统更新中心”，集中展示当前版本、GitHub 最新版本、仓库地址、更新日志和最近检查时间。
  - 支持从 GitHub `version.json` 获取版本元数据，并在后台显示是否存在新版本。
- 新增更新计划、预检和人工命令清单：
  - 支持按远端 release archive 生成更新计划，预览新增、修改、删除、迁移、依赖和手动步骤。
  - 对仓库来源、更新包大小、文件路径、磁盘空间、工作区状态、备份状态和执行开关做预检。
  - 后台提供可复制的人工命令清单，默认不直接执行宿主机 shell。
- 新增备份与回滚闭环：
  - 支持更新前备份待替换文件，记录备份 manifest、来源版本、目标版本、文件数量和备份路径。
  - 支持单文件恢复、整包回滚、回滚预检和最近 10 次备份保留策略。
  - 回滚执行默认受环境开关和超级管理员密码保护。
- 新增队列化更新执行和恢复能力：
  - 更新与回滚通过后台队列执行，记录执行阶段、时间线、日志、验收命令和失败原因。
  - 支持状态接口轮询、卡住任务提示、失败任务重试和手动标记失败。
  - 执行前验证文件仍匹配计划，降低部分替换和并发更新风险。
- 新增部署诊断与自救模块：
  - 系统更新中心新增 APP_URL、APP_KEY、数据库、迁移表、`storage/app` 和 `bootstrap/cache` 可写性检查。
  - 展示当前运行配置、Laravel 日志摘要和适配部署模式的命令向导。
  - 针对 Ubuntu 24.04 LTS + Docker 生产部署，新增初始化命令、`.env.prod` 生效检查、容器日志和 500 排障说明。
- 更新后台版本号为 `2.0.3`，同步 `version.json` 和后台默认版本展示。

## 2026-05-30

### 分发管理

- 新增通用 HTTP API 分发渠道：
  - 支持无鉴权、Bearer Token、Basic Auth、自定义 Header Key 和 HMAC 签名。
  - 支持按动作配置健康检查、发布、更新、删除和站点设置同步的 HTTP 方法与路径。
  - 支持 `remote_id` / `remote_url` 响应字段映射、成功状态码配置、Payload 包装和请求超时配置。
  - 通用 API 渠道复用现有分发队列、重试、日志、远端文章编辑/删除和站点设置同步能力。
- 分发渠道详情页补充通用 API 接入引导、响应映射摘要和示例 Payload，便于第三方系统实现接收端。
- README 与多语言 README 更新分发能力描述，补充通用 HTTP API 渠道。

## 2026-05-28

### v2.0.2

- 后台首页升级为“GEOFlow 自动化工作流”面板：
  - 用流程节点展示 API、素材库、任务、文章、分发、数据分析和站点设置之间的自动化关系。
  - 保留三步完成配置和配套 Skill 工具入口，减少首页重复统计卡片。
- 提升数据分析统计真实性：
  - 总浏览量、内容浏览、Top 内容和日志分析优先基于 `view_logs` 事件数据统计，并过滤非 GET 请求。
  - 发布趋势使用实际 `published_at`，分发指标按任务和分类筛选时同步约束关联文章。
  - 统一 AI 爬虫、搜索蜘蛛、其他自动化访问和人类访问的识别规则，减少误判。
- 改善本地 Docker 开发体验：
  - 开发镜像禁用 CLI OPcache 缓存，避免挂载代码更新后后台仍显示旧页面。
- 更新后台版本号为 `2.0.2`，同步 `version.json`、环境变量样例和后台默认版本展示。

## 2026-05-24

### AI 模型与知识库

- 新增 Gemini 原生模型接入能力：
  - 支持 Gemini chat 与 embedding 模型配置，不再只依赖 OpenAI-compatible 路由。
  - 后台模型列表、连接测试和任务生成链路统一识别 Gemini Provider。
- 新增知识库切片策略配置：
  - 支持结构化规则切片、自动策略和可选 LLM 语义规划。
  - LLM 只用于规划语义边界，最终切片从原文稳定重建，并保留规则切片作为回退。
  - 切片元数据补充标题、章节路径、策略、序号和 source hash，便于预览、排查和后续重建。

### 任务与分发

- 优化任务创建与编辑页面：
  - 表单宽度与任务管理列表对齐，减少左右空白。
  - 内容配置、素材、分发范围等区域按更宽布局重新排版。
- 修复“仅发布到本站”仍可勾选渠道的问题：
  - 前端选择该模式后会禁用并清空渠道站点选择。
  - 后端在 `local_only` 发布范围下忽略旧提交中的 `distribution_channel_ids`，避免误入远程分发队列。

### 文档

- 更新仓库首页 README 与多语言 README，补充 Gemini、语义切片、WordPress REST 渠道和发布范围控制。
- 更新 Wiki 中英文大纲，新增分发管理、数据分析、知识库切片与 RAG 相关说明页。

## 2026-05-23

### 分发管理

- 新增 WordPress REST API 分发渠道设计与实现：
  - 支持 WordPress Application Password 鉴权，密钥加密保存且不明文回显。
  - 支持文章发布、更新、删除、图片上传到媒体库、分类/标签同步和基础站点设置同步。
  - 分发渠道后台按 GEOFlow Agent 与 WordPress REST 渠道展示不同配置项和接入引导。
  - WordPress 渠道复用统一分发队列、远端元数据、健康检查、远端编辑/删除和日志记录能力。

### 文档

- 系统性更新仓库首页 README 与多语言 README：
  - 首屏描述从“未来多端分发”更新为当前已支持的 GEO 内容工程与多站点分发系统。
  - 功能表补充数据分析、分发管理、目标站点包、静态页面分发、`llms.txt` / TXT 地图、远端站点设置同步和 LLM 抓取友好输出。
  - 运行结构与系统架构补充目标站点 Agent、分发队列、远端静态页面和日志分析链路。

## 2026-05-22

### v2.0.1

- 新增分发管理可运行闭环：
  - 后台新增分发渠道列表、新建、编辑、详情、队列、日志、测试连接、暂停/启用、重置密钥和远端文章管理入口。
  - 渠道密钥支持创建后一次性展示，并允许超级管理员通过当前登录密码临时验证查看，降低部署过程中的密钥丢失风险。
  - 任务和文章支持绑定分发渠道，本地发布后可自动进入分发队列，文章列表与任务列表可见分发状态。
  - 分发队列支持远端副本编辑与删除；编辑远端文章时同步反写 GEOFlow 本地文章，删除远端副本后自动刷新远端列表页和地图文件。
- 新增目标渠道站点包与静态站点模式：
  - 渠道详情页可下载按当前渠道密钥、站点设置和部署路径预配置的目标站点包。
  - 目标站点包内置 PHP Agent、首页、文章详情页、静态资源目录、站点地图、TXT 地图、Apache `.htaccess` 和 Nginx 伪静态参考规则。
  - 静态模式默认启用，发布或删除文章时生成静态首页、详情页、sitemap 和适用于 LM 抓取的 TXT 地图文件。
  - 文章详情页补充 Markdown 排版、表格、代码块、引用、图片渲染、Schema 结构化数据和独立 CSS 资源引用。
- 新增远端站点设置同步能力：
  - 分发渠道编辑页可维护目标站标题、副标题、站点描述、版权、备案号、主题模板、分类等远端站点信息。
  - 新增“更新目标站点”操作，用于在重新上传目标站点包或修改站点设置后批量同步首页、文章页、地图文件和远端配置。
  - 新增静态模式与伪静态模式说明，并在后台提供 Apache/Nginx 规则复制入口。
- 新增数据分析页面：
  - 顶部菜单新增“数据分析”，集中展示系统总览、单站运营、多站分发和自助日志数据。
  - 支持日期范围、快捷时间、分发渠道、任务、分类、文章、流量类型和日志来源筛选；快捷时间选择后需点击“应用筛选”再刷新数据。
  - 内容运营分析展示发布趋势、任务趋势、内容漏斗、分类分布和任务/素材/AI 健康状态。
  - 日志分析支持访问趋势、Top 文章、Top 渠道站点、AI 爬虫识别、状态码、来源类型和示例访问日志可视化。
- 优化后台首页为导航型入口：
  - 首页移除原有数据统计卡片，将统计能力集中到数据分析页。
  - 保留三步完成配置引导，并按“单站点运营”“多站点分发”“配套 Skill 资源”组织常用功能入口。
  - 单站点运营入口补充提示词配置和用户管理；多站点分发入口补充目标站点包、分发队列、日志和相关 skill 导航。
- 优化首次部署说明：
  - `GEOFlow 2.0 首次部署说明` 改为更紧凑的白底 Kami 文档风格，降低标题和正文字号。
  - 文案补充后台首页、数据分析、单站点运营、多站点分发和上线前备份检查说明。
- 完善葡萄牙语后台本地化：
  - 吸收并补齐 PR #27 的 `pt_BR` 后台翻译，覆盖导航、通知、作者、前台、素材、AI 配置、数据分析和分发管理等当前全部后台语言键。
  - 新增葡萄牙语覆盖测试，防止新后台模块继续回退到英文文案。
- 吸收 Docker 部署 PR 的低风险改进：
  - Docker 开发与生产 compose 支持通过环境变量配置 PHP、Composer、Nginx、pgvector、Redis 基础镜像和 Composer Packagist 镜像。
  - `.dockerignore` 排除本地 Docker 数据、日志、缓存、session、视图缓存和上传目录，避免构建镜像时带入运行态数据。
  - 补充默认管理员 Seeder 创建与不覆盖已有账号的测试覆盖。
- 增强测试覆盖：
  - 新增分发管理、数据分析、访问日志、活动日志脱敏、欢迎说明、迁移结构和重试策略相关测试。
  - 本次发布前完整测试结果为 `188 passed`、`1231 assertions`。

## 2026-05-21

### v2.0

- 后台版本号更新为 `2.0`，同步更新 `version.json`、环境变量样例和后台版本展示默认值。
- 后台首次安装/部署后的欢迎弹窗改为“首次部署说明”：
  - 提醒管理员先完成密码、后台入口、站点 URL、语言和基础安全检查
  - 引导检查 PostgreSQL、Redis、队列、调度器和存储目录权限
  - 明确第一轮上手流程：配置模型与提示词、准备素材、小样本生成、审核发布、再开启批量任务
- 补充分发管理 2.0 的首次使用说明：
  - 说明目标渠道、Agent 地址、密钥、静态模式和目标站点包的关系
  - 引导下载目标站点包、测试连接、同步远端设置和查看分发日志
  - 强调升级或迁移前备份数据库、`.env`、上传目录、`storage` 和目标渠道站点包

## 2026-05-10

### v1.2.x

- 优化第三方 AI 标题生成兼容性：
  - 标题库 AI 生成链路不再硬编码 `openai` driver
  - 根据 API Base URL 与模型 ID 自动选择运行时 driver
  - 避免 DeepSeek、智谱、MiniMax、火山方舟、阿里百炼等 OpenAI-compatible 接口被误调 `/v1/responses` 导致 404
- 增强 URL 智能采集安全配置：
  - URL 采集 SSRF 防护默认保持严格模式
  - 新增 `URL_IMPORT_ALLOW_MIXED_DNS=false` 示例配置，仅用于明确受控的透明代理、Docker 或 VPN 混合 DNS 环境
  - 业务代码统一读取 `config('geoflow.url_import_allow_mixed_dns')`，兼容 Laravel 配置缓存
- 补充模型 driver 识别与 URL 标准化测试覆盖。
- 修复生产 Docker 首次部署默认管理员初始化：
  - `docker-compose.prod.yml` 的一次性 `init` 服务在迁移后运行 `geoflow:install`
  - 首次空库安装才写入默认后台账号，已有数据的旧库只补初始化标记
  - 常驻服务不接收初始化环境变量，重启时不会重复执行安装填充

## 2026-05-08

### v1.2.x

- 新增 AI 模型连接测试能力：
  - 后台 AI 模型列表支持直接测试模型 API 连通性
  - 覆盖聊天模型与 embedding 模型的基础连接验证
  - 测试失败时返回具体错误，便于定位 API Key、endpoint、模型 ID 和 provider 配置问题
- 优化前端与后台资源加载稳定性：
  - 将前台模板依赖的 Tailwind Play CDN 与 Lucide 外部 CDN 替换为本地托管资源
  - 降低国内网络环境下 CDN 不稳定导致页面样式或脚本加载失败的风险
- 新增一键部署脚本与部署文档：
  - 新增 `deploy-scripts/`，提供 Docker 部署、服务器自检和部署后健康检查脚本
  - Wiki 同步补充部署指南、服务器配置建议和部署脚本使用说明
- 修复任务删除兼容性：
  - 删除任务时不再依赖旧版 `article_queue` 表
  - 避免新版本数据库结构下删除任务触发 `Undefined table: article_queue` 错误
- 优化任务创建 API 的可选素材字段处理：
  - API 创建任务时允许省略作者、图片库、知识库和固定分类等可选字段
  - 省略字段会显式写入 `null`，避免接口契约与后台创建任务逻辑不一致
  - 新增接口测试覆盖可选素材字段省略场景
- 新增网易新闻风格前台主题：
  - 新增 `netease-news-20260429` 前台主题
  - 首页、分类页与详情页采用更接近资讯站的两栏阅读布局
  - 保留 GEOFlow 文章、分类、作者、SEO 与 Schema 数据调用规则
- 新增 TDWH 英文主题分支：
  - 新增 `tdwh-english-20260501` 英文主题样板
  - 面向英文内容站点提供更清晰的国际化首页、列表页和详情页结构

## 2026-05-06

### v1.2.x

- 修复任务生成文章时的作者兜底逻辑：
  - 任务未配置作者时，自动使用系统中已有作者
  - 任务配置的作者已不存在时，自动回退到可用作者
  - 系统没有任何作者时，自动创建默认作者 `GEOFlow`
  - 避免向 `articles.author_id` 写入 `null` 导致 PostgreSQL `NOT NULL` 约束错误
- 优化 `URL 智能采集` 的 AI 解析兼容性：
  - 当某个 AI 模型失败时，继续尝试下一个可用模型
  - 关键词与标题阶段支持解析 AI 返回的纯文本列表，降低非标准 JSON 输出导致的失败率
  - 错误信息保留具体模型与失败原因，便于排查 API Key、模型响应格式和 provider 配置问题
- 升级后台仪表盘：
  - 新增任务、素材、AI 模型、URL 采集、热门内容等数据概览模块
  - 调整“快速开始”与趋势模块位置，让后台首页更接近运营仪表盘
  - 修复“本周数据趋势”与下方健康模块间距过窄的问题
- 稳定本地运行态：
  - 修复后已清理 Laravel optimize cache，并重启 app / queue / scheduler 容器
  - 新增任务作者兜底测试，覆盖空作者、失效作者和无作者初始化场景

## 2026-04-18

### v1.2

- 新增后台与前台第一阶段中英界面支持：
  - 后台正式管理页支持中英切换
  - 登录页支持独立语言选择
  - 前台公共壳子跟随后台语言显示
- 新增任务 `智能模型切换`：
  - 任务支持 `固定模型` 与 `智能模型切换`
  - 主模型失败时，系统按模型优先级自动尝试下一个可用聊天模型
- 优化模型接入规则：
  - 支持 OpenAI、DeepSeek、MiniMax、智谱 GLM、火山方舟等不同版本化 chat / embedding endpoint
  - 后台模型配置支持基础地址或完整接口
- 优化任务执行体验：
  - `task-execute.php` 改为入队执行，不再同步阻塞页面
  - 直接发布任务的 `published_count` 统计已修正
- 新增前台模板预览与启用能力：
  - 支持独立 `preview/<theme-id>` 动态预览路由
  - 支持主题包 `themes/<theme-id>` 结构
  - 后台网站设置支持模板预览与启用
  - 样板主题 `qiaomu-editorial-20260418` 已进入公开仓库
  - 首页、分类页、归档页卡片摘要会自动清洗 Markdown 符号
- 新增后台首次登录欢迎页：
  - 首次登录后自动弹出欢迎页
  - 欢迎页改为单篇“见面信”结构，默认中文，可切英文
  - footer 新增 `项目说明` 入口，可重新打开欢迎页
  - 新增实现说明文档 `project/ADMIN_WELCOME.md`
- 新增 `geoflow-template` 配套 skill 入口：
  - 用于把参考网址映射为 GEOFlow 兼容主题包
  - 支持输出 `tokens.json`、`mapping.json` 和 preview-first 模板规划
- 升级默认 GEO 提示词：
  - 正文、榜单、关键词、描述提示词更新为长版模板
  - 对齐 GeoFlow 变量规则
- 修复若干后台可用性问题：
  - 数据库时区偏差
  - 文章图片路径缺少前导 `/`
  - 标题 AI 保存时的 PostgreSQL 布尔类型写入错误
  - Provider 默认示例从旧的第三方域名改为更中性的 DeepSeek

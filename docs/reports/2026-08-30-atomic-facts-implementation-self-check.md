# 原子事实 1—4 阶段实施自检与修复清单

## 已交付

- [x] 独立工作台路由、分页、搜索、状态筛选、跨页合并目标、审核、冲突、版本和发布准备度。
- [x] 知识库详情改为摘要入口，展示事实数、待审核、冲突、活动版本和服务状态。
- [x] 生成弹窗支持真实进度、候选数、冲突数、耗时、取消、刷新恢复、失败引导和完成后审核。
- [x] 类型化比较器覆盖数值、数量级、百分比、日期、范围、布尔、URL、路径、版本和文本。
- [x] 正式原子路由在知识检索前运行，只允许来源哈希一致且 `ready` 的活动 revision。
- [x] 主张先截取 24 条，再按主体、谓词和别名召回事实，避免按 manifest 顺序截断关键事实。
- [x] 多值事实执行时间与范围适用性选择；无法唯一确定时进入 ambiguous 和知识库回退。
- [x] 已覆盖主张从知识库回退内容中移除；确定性版本冲突进入统一问题聚合与评分。
- [x] 结果页展示业务模式、revision、覆盖率、命中/冲突/回退、分段 Token、比较方法和证据。
- [x] 质检指纹包含活动 revision、库哈希、源哈希和服务状态。
- [x] 发布和恢复 revision 后使关联文章质检结果失效；知识切片重新同步时同步标记事实库 stale。
- [x] rollout 增加原子影子、正式比例和原子冻结；正式与影子共用稳定分桶。
- [x] 本地报告门禁绑定 KB23、模型 3、固定文章 ID、3 次重复、case-set 哈希、活动 revision 和 24 小时时效。
- [x] 评测命令支持断点检查点、受控单次重试、AB/BA/AB 顺序、30 次原始结果与正式报告。
- [x] KB23 的 20 条事实完成逐条审核、证据定位和 v1 发布。

## 本轮审查发现并已修复

- [x] 修复工作台分页后跨页合并和发布准备度只读取当前页的问题。
- [x] 修复正式生产仍先执行完整知识库质检、评测另走精简模拟路径的问题。
- [x] 修复第 25 条以后关键事实无法被召回的问题。
- [x] 修复多值事实固定选择第一项、忽略时间与范围的问题。
- [x] 修复知识源更新到事实库变 stale 之间的时间窗口。
- [x] 修复 revision 切换后历史文章结果未失效的问题。
- [x] 修复本地灰度报告可被任意五篇文章或手工简化 JSON 冒用的问题。
- [x] 修复正式与影子灰度使用不同分桶的问题。
- [x] 修复原子冻结仍继续执行影子核验的问题。
- [x] 修复生成轮询鉴权失败时重新启用提交按钮、取消无超时、服务端可操作错误未显示的问题。
- [x] 修复工作台嵌套 `main`、移动端合并表单溢出、葡萄牙语弹窗缺失和页面注册缺失。
- [x] 修复 30 次评测中模型瞬时失败会丢失全部已完成结果的问题。

## 验证结果

- PHP 相关测试：AtomicFact、KnowledgeFactLibrary、ArticleAiQualityRollout、ArticleAiQualityInspectionService、Admin UI 路由与整页 smoke 全部通过。
- JavaScript：171 项全部通过。
- 前端构建：Vite production build 通过。
- 路由：`admin.knowledge-bases.facts.index` 注册通过。
- 格式：Pint dirty 通过，`git diff --check` 通过。
- 全量 PHP：2249 通过、1 跳过、4 个与本迭代无关的既有环境/隔离失败；分别为容器缺少 `rsync`、主题复制 fake disk fixture、媒体条件请求返回 304、大小写敏感文件系统断言。
- 桌面页面：在当前认证浏览器自然视口 1512×712 验证，无横向溢出，工作台数据与活动 revision 正确。
- 容器：app、knowledge queue、AI quality queue 两个副本、AI quality backfill queue 已重启；没有重建镜像。
- 真实评测：30 次成功调用完成，原始 JSON 与正式 Markdown 已落盘。

## 真实评测后保留的改进项

- [ ] 文本类 supported 召回偏低，安全文章 90.83% 主张仍回退知识库；需补充集合型 contains/subset 和高置信别名规则。
- [ ] ambiguous 文本目前由现有知识库质检链路处理；一次最多 16 组的专用批量语义核验仍需独立轻量提示词，当前正式比例保持 0 可避免风险扩散。
- [ ] 通用模型对安全文章的 passed/needs_review 有波动，需要校准覆盖门禁和不确定性输出。
- [ ] 原子输入 Token 降幅 6.94%，需要缩短正式原子模式的通用提示词并提高确定性覆盖。
- [ ] 生成任务 dispatch 与 batch ID 持久化之间仍有极短的进程崩溃窗口；现有刷新恢复覆盖普通中断，后续可用 outbox/reservation 完全消除。
- [ ] 390×844 的隔离浏览器像素级复核未在当前会话建立独立认证上下文；响应式布局、键盘与对话框行为已有 Feature/JavaScript 契约覆盖。

## 灰度与回滚状态

- 正式原子比例：0%。
- 原子影子比例：100%，已按 10 → 25 → 50 → 100 记录四次事件。
- 全局冻结：关闭；原子冻结：关闭。
- 活动 revision：KB23 / revision 1 / ready。
- 需要立即停止原子核验时，执行 `php artisan geoflow:ai-quality-rollout freeze --track=atomic-fact`。
